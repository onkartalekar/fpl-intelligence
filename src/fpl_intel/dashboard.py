"""Self-contained local FPL preseason and in-season decision workspace."""

import json

from .transfers import OFFICIAL_CLUB_DOMAINS


_TRUSTED_LINK_DOMAINS = sorted(
    OFFICIAL_CLUB_DOMAINS | {"premierleague.com", "fantasy.premierleague.com"}
)


_TEMPLATE = r'''<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<meta name="refresh-token" content="__REFRESH_TOKEN__">
<title>FPL Intelligence Workspace</title>
<style>
:root{--bg:#08101f;--nav:#0b1526;--panel:#101b2e;--panel2:#17253d;--line:#293a58;--text:#f3f6ff;--muted:#9eacc3;--accent:#57dfae;--blue:#83c4ff;--warn:#ffd477;--danger:#ff8998;--low:#71809c}
*{box-sizing:border-box}html{color-scheme:dark}body{margin:0;background:var(--bg);color:var(--text);font:14px/1.5 ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;font-variant-numeric:tabular-nums}button,input,select,summary{font:inherit}button{color:inherit}a{color:#91ceff}button:focus-visible,input:focus-visible,select:focus-visible,summary:focus-visible,a:focus-visible{outline:2px solid var(--accent);outline-offset:2px}.app{min-height:100vh;display:grid;grid-template-columns:238px minmax(0,1fr)}.sidebar{background:var(--nav);border-right:1px solid var(--line);padding:24px 15px;position:sticky;top:0;height:100vh}.brand{padding:0 10px 20px;border-bottom:1px solid var(--line)}.brand strong{font-size:18px;display:block}.brand span,.eyebrow,.muted{color:var(--muted)}.eyebrow{text-transform:uppercase;letter-spacing:.12em;font-size:11px;font-weight:750}.nav{display:grid;gap:4px;margin-top:18px}.nav button{min-height:44px;background:transparent;border:1px solid transparent;border-radius:8px;text-align:left;padding:10px 11px;cursor:pointer}.nav button:hover{background:var(--panel2)}.nav button.active{background:var(--panel2);border-color:var(--line);box-shadow:inset 3px 0 var(--accent);color:var(--accent)}.mobile-nav-wrap{display:none}.content{padding:28px 32px;min-width:0;max-width:1500px;width:100%;margin:0 auto}.topbar{display:flex;justify-content:space-between;align-items:flex-start;gap:24px;margin-bottom:19px}.topbar h1{font-size:27px;line-height:1.2;margin:3px 0 4px}.deadline{color:var(--muted);margin-top:5px}.header-actions{display:flex;align-items:center;justify-content:flex-end;gap:9px;flex-wrap:wrap;max-width:490px}.pill{display:inline-flex;align-items:center;min-height:38px;border:1px solid var(--warn);color:var(--warn);padding:7px 11px;border-radius:999px;font-size:12px;font-weight:700}.refresh-button{min-height:44px;background:var(--accent);border:1px solid var(--accent);color:#062219;font-weight:800;padding:9px 14px;border-radius:9px;cursor:pointer}.refresh-button:hover{filter:brightness(1.08)}.refresh-button:disabled{opacity:.55;cursor:wait}.refresh-message,.refresh-source-status{flex-basis:100%;text-align:right;color:var(--muted);font-size:12px;min-height:17px}.refresh-source-status{color:var(--blue)}.view{display:none}.view.active{display:block}.attention-panel{background:linear-gradient(135deg,#132239,#0f1b30);border:1px solid #385171;border-radius:12px;padding:19px;margin-bottom:14px}.section-heading{display:flex;align-items:baseline;justify-content:space-between;gap:12px;margin-bottom:11px}.section-heading h2{font-size:16px;margin:0}.attention{display:grid;gap:8px}.attention-item{display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:start;gap:11px;padding:12px 0;border-top:1px solid var(--line)}.attention-item:first-child{border-top:0}.severity{display:inline-flex;align-items:center;justify-content:center;min-width:68px;border-radius:999px;padding:4px 8px;font-size:10px;font-weight:800;text-transform:uppercase;letter-spacing:.06em;background:#4c3f1c;color:#ffe39c}.severity.info{background:#203b59;color:#b9dcff}.severity.ready{background:#164b3a;color:#94efcb}.attention-item strong{display:block}.attention-action{background:transparent;border:0;color:var(--blue);min-height:36px;cursor:pointer;padding:5px 0}.metrics{display:grid;grid-template-columns:1.35fr repeat(3,1fr);gap:10px;margin:14px 0}.metric{background:var(--panel);border-top:1px solid var(--line);padding:14px 15px}.metric:first-child{border-radius:10px 0 0 10px}.metric:last-child{border-radius:0 10px 10px 0}.metric b{display:block;font-size:21px;line-height:1.25}.metric.primary b{font-size:17px;color:var(--warn)}.metric span{font-size:12px;color:var(--muted)}.grid{display:grid;grid-template-columns:minmax(0,1.12fr) minmax(320px,.88fr);gap:14px}.overview-stack{display:grid;gap:14px;align-content:start}.panel{background:var(--panel);border:1px solid rgba(70,91,124,.58);border-radius:11px;padding:18px;min-width:0}.panel h2{font-size:15px;margin:0 0 12px}.panel h3{font-size:14px;margin:0}.change-summary{display:grid;gap:7px}.change-row{display:flex;justify-content:space-between;gap:20px;padding:8px 0;border-top:1px solid var(--line)}.change-row:first-child{border-top:0}.change-value{font-weight:800;color:var(--accent)}.club-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.club-card{min-height:58px;background:var(--panel2);border:1px solid transparent;border-radius:8px;padding:10px 11px;text-align:left;cursor:pointer}.club-card:hover,.club-card:focus-visible{border-color:var(--accent)}.club-card strong{display:block}.club-card small{color:var(--muted)}.steps{display:grid;gap:0}.step{padding:11px 0;border-top:1px solid var(--line)}.step:first-child{border-top:0}.filter-shell{position:sticky;top:0;z-index:3;background:rgba(8,16,31,.97);border-block:1px solid var(--line);padding:11px 0;margin:14px 0}.toolbar{display:grid;grid-template-columns:minmax(220px,1.5fr) minmax(150px,1fr) minmax(160px,1fr);gap:9px}.field label{display:block;color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.06em;margin:0 0 4px}.field input,.field select{width:100%;min-height:44px;background:var(--panel2);border:1px solid var(--line);color:var(--text);border-radius:8px;padding:9px 10px}.more-filters{margin-top:9px}.more-filters summary{width:max-content;min-height:40px;display:flex;align-items:center;color:var(--blue);cursor:pointer}.more-grid{display:grid;grid-template-columns:repeat(3,minmax(140px,1fr));gap:9px;padding:7px 0}.filter-meta{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:8px}.active-filters{display:flex;gap:6px;flex-wrap:wrap}.filter-chip{min-height:32px;border:1px solid var(--line);background:var(--panel2);border-radius:999px;padding:4px 9px;cursor:pointer;font-size:12px}.reset-filters{min-height:36px;background:transparent;border:0;color:var(--blue);cursor:pointer}.transfer-layout{display:grid;grid-template-columns:minmax(0,1.4fr) minmax(290px,.6fr);gap:14px}.feed{display:grid;gap:7px}.transfer{width:100%;min-height:74px;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;text-align:left;background:var(--panel2);border:1px solid transparent;border-radius:8px;padding:12px;cursor:pointer}.transfer:hover,.transfer:focus-visible{border-color:var(--accent)}.transfer strong{display:block;font-size:15px}.transfer small,.why{color:var(--muted)}.why{display:block;margin-top:4px;font-size:12px}.tags{display:flex;gap:5px;flex-wrap:wrap;margin-top:7px}.tag{font-size:10px;padding:3px 7px;border-radius:999px;background:#203653;color:#c0ddf7}.tag.high{background:#164b3a;color:#8ff0c7}.tag.medium{background:#493d1d;color:#ffe19a}.tag.low{background:#283044;color:#aebbd2}.inspector{align-self:start;position:sticky;top:135px}.pagination{display:flex;align-items:center;justify-content:space-between;gap:10px;margin-top:12px}.pagination button{min-height:44px;background:var(--panel2);border:1px solid var(--line);border-radius:8px;padding:8px 12px;cursor:pointer}.pagination button:disabled{opacity:.35;cursor:default}.empty{border:1px dashed var(--line);border-radius:8px;padding:20px;color:var(--muted)}dl{display:grid;grid-template-columns:130px 1fr;gap:8px 10px;margin:0}dt{color:var(--muted)}dd{margin:0;overflow-wrap:anywhere}.placeholder{padding:27px;border:1px dashed var(--line);background:var(--panel);border-radius:11px}.placeholder h2{margin-top:0}.team-summary{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.team-stat{background:var(--panel2);border-radius:8px;padding:12px}.team-stat b{display:block;font-size:18px}.team-stat span{color:var(--muted);font-size:12px}.squad-grid{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:8px}.player-card{background:var(--panel2);border-radius:8px;padding:11px;min-height:74px}.player-card strong,.player-card span{display:block}.player-card span{color:var(--muted);font-size:12px}.player-toolbar{display:grid;grid-template-columns:1.4fr repeat(3,minmax(150px,1fr));gap:9px;margin:16px 0}.player-table{width:100%;border:1px solid var(--line);border-radius:9px;border-collapse:separate;border-spacing:0;overflow:hidden}.player-head{background:var(--panel2);color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.06em}.player-table th,.player-table td{padding:11px 13px;text-align:left;border-top:1px solid var(--line)}.player-table thead th{border-top:0}.player-row strong,.player-row small{display:block}.player-row small{color:var(--muted)}.price{font-weight:800;color:var(--accent)}.fixture-toolbar{display:grid;grid-template-columns:repeat(2,minmax(180px,260px));gap:9px;margin:16px 0}.fixture-list{display:grid;gap:8px}.fixture-row{display:grid;grid-template-columns:150px minmax(0,1fr) auto minmax(0,1fr);gap:14px;align-items:center;background:var(--panel2);border-radius:9px;padding:13px}.fixture-team{display:flex;align-items:center;justify-content:space-between;gap:10px}.fixture-team.away{flex-direction:row-reverse}.difficulty{display:inline-flex;align-items:center;justify-content:center;min-width:30px;height:30px;border-radius:6px;background:#263a57;font-weight:800}.difficulty.d1,.difficulty.d2{background:#175442;color:#a1f3d1}.difficulty.d4,.difficulty.d5{background:#573040;color:#ffc1cb}.profile-options{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px}.profile-option{min-height:92px;background:var(--panel2);border:1px solid var(--line);border-radius:9px;padding:12px;text-align:left;cursor:pointer}.profile-option:hover{border-color:var(--blue)}.profile-option.active{border-color:var(--accent);box-shadow:inset 3px 0 var(--accent)}.profile-option strong,.profile-option span{display:block}.profile-option strong{font-size:15px}.profile-option span{font-size:11px;color:var(--muted);margin-top:3px}.profile-comparison{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:8px;margin-top:10px}.profile-stat{background:#0d1829;border-radius:8px;padding:10px}.profile-stat b,.profile-stat span{display:block}.profile-stat b{color:var(--blue);font-size:16px}.profile-stat span{color:var(--muted);font-size:10px}.profile-explanation{grid-column:1/-1;border-top:1px solid var(--line);padding-top:9px;color:var(--muted)}.scenario-options{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px;margin-top:12px}.scenario-card{background:var(--panel2);border:1px solid var(--line);border-radius:9px;padding:12px}.scenario-card.recommended{border-color:var(--accent);box-shadow:inset 3px 0 var(--accent)}.scenario-card strong,.scenario-card span{display:block}.scenario-card span{color:var(--muted);font-size:11px;margin-top:4px}.scenario-card .projection{color:var(--blue);font-weight:700}.weekly-recommendation,.weekly-chip{margin-top:12px;padding:13px;border-radius:9px;background:#0d1829;border:1px solid var(--line)}#weekly-profile-options:empty,#weekly-summary:empty,#weekly-scenarios:empty,#weekly-chip:empty,#weekly-rules:empty{display:none}.weekly-plan{margin-top:14px;padding:14px;border:1px solid var(--line);border-radius:10px;background:#0b1728}.weekly-plan-summary{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.weekly-plan-stat{padding:10px;border-radius:8px;background:var(--panel2)}.weekly-plan-stat b,.weekly-plan-stat span{display:block}.weekly-plan-stat b{color:var(--accent);font-size:17px}.weekly-plan-stat span{color:var(--muted);font-size:11px}.conditional-branches{display:grid;gap:8px;margin-top:11px}.conditional-branch{padding:10px 11px;border-left:3px solid var(--blue);border-radius:7px;background:var(--panel2)}.conditional-branch strong,.conditional-branch span{display:block}.conditional-branch span{color:var(--muted);font-size:11px;margin-top:3px}.planner-assumptions{margin-top:10px;color:var(--muted)}.weekly-recommendation:has(.empty){background:transparent;border:0;padding:0}.weekly-recommendation strong,.weekly-chip strong{color:var(--accent)}.transfer-pair{padding:7px 0;border-top:1px solid var(--line)}.transfer-pair:first-child{border-top:0}.transfer-pair strong,.transfer-pair span{display:block}.weekly-lineup{margin-top:14px;padding-top:14px;border-top:1px solid var(--line)}.weekly-lineup .formation-pitch{min-height:460px}.weekly-bench{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:9px}.weekly-bench-card{background:var(--panel2);border-radius:8px;padding:9px}.weekly-bench-card strong,.weekly-bench-card span{display:block}.weekly-bench-card span{font-size:11px;color:var(--muted)}.chip-alternatives{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-top:9px}.chip-alternative{background:var(--panel2);border-radius:7px;padding:9px;font-size:11px}.formation-pitch{position:relative;isolation:isolate;min-height:520px;display:flex;flex-direction:column;justify-content:space-around;gap:16px;padding:28px 16px;border:2px solid rgba(255,255,255,.34);border-radius:10px;overflow:hidden;background:linear-gradient(90deg,rgba(255,255,255,.035) 50%,transparent 50%),repeating-linear-gradient(0deg,#15583f 0,#15583f 52px,#185f45 52px,#185f45 104px);background-size:100% 100%,100% 100%}.formation-pitch:before{content:"";position:absolute;z-index:-1;inset:50% 0 auto;height:2px;background:rgba(255,255,255,.34)}.formation-pitch:after{content:"";position:absolute;z-index:-1;left:50%;top:50%;width:112px;height:112px;border:2px solid rgba(255,255,255,.34);border-radius:50%;transform:translate(-50%,-50%)}.pitch-row{display:flex;align-items:center;justify-content:space-evenly;gap:8px;position:relative;z-index:1}.pitch-player{width:min(130px,19%);min-width:0;background:rgba(9,21,34,.92);border:1px solid rgba(255,255,255,.24);border-radius:8px;padding:8px 6px;text-align:center;box-shadow:0 4px 12px rgba(0,0,0,.26)}.pitch-player.captain{border-color:var(--accent);box-shadow:0 0 0 2px rgba(87,223,174,.22)}.pitch-player strong,.pitch-player span{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.pitch-player strong{font-size:12px}.pitch-player span{font-size:10px;color:#d0d9e8}.pitch-player .projection{color:#a8d7ff;font-weight:800}.decision-hero{border-color:#356c61;background:linear-gradient(135deg,#112b2a,#101b2e)}.decision-summary{display:grid;grid-template-columns:repeat(5,minmax(120px,1fr));gap:9px;margin:13px 0}.decision-metric{background:var(--panel2);border-radius:8px;padding:11px}.decision-metric b{display:block;font-size:19px;color:var(--accent)}.decision-metric span{display:block;color:var(--muted);font-size:11px}.decision-layout{display:grid;align-items:start;grid-template-columns:minmax(0,1.4fr) minmax(300px,.6fr);gap:14px;margin-top:14px}.recommendation-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.recommendation-grid.compact{grid-template-columns:repeat(4,minmax(0,1fr))}.recommendation-card{background:var(--panel2);border:1px solid transparent;border-radius:8px;padding:10px;min-width:0}.recommendation-card.captain{border-color:var(--accent)}.recommendation-card strong,.recommendation-card span{display:block}.recommendation-card span{color:var(--muted);font-size:11px}.recommendation-card .projection{color:var(--blue);font-weight:700;margin-top:5px}.decision-list{display:grid;gap:7px}.decision-row{display:flex;justify-content:space-between;gap:12px;padding:9px 0;border-top:1px solid var(--line)}.decision-row:first-child{border-top:0}.decision-note{padding:9px 0;border-top:1px solid var(--line);color:var(--muted)}.decision-note:first-child{border-top:0}.source-list{display:grid;gap:7px}.source{display:flex;justify-content:space-between;gap:12px;padding:10px 0;border-top:1px solid var(--line)}.source:first-child{border-top:0}.source span{color:var(--accent)}footer{color:var(--muted);font-size:12px;padding:20px 0}.countline{display:flex;justify-content:space-between;gap:12px;color:var(--muted);margin:6px 0 11px}.status-good{color:var(--accent)}.status-wait{color:var(--warn)}.limitation-note{margin-top:12px;padding:11px 12px;border:1px solid rgba(255,212,119,.38);border-radius:9px;background:rgba(255,212,119,.07);color:var(--muted)}.limitation-note strong{display:block;margin-bottom:3px;color:var(--warn)}.performance-table{width:100%;min-width:780px;border:1px solid var(--line);border-radius:9px;border-collapse:separate;border-spacing:0}.performance-table th,.performance-table td{padding:11px 13px;text-align:left;border-top:1px solid var(--line)}.performance-head{background:var(--panel2);color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.05em}.performance-head th{border-top:0}.performance-row .positive{color:var(--accent)}.performance-row .negative{color:var(--danger)}.performance-horizons{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px}.performance-card{background:var(--panel2);border-radius:8px;padding:12px}.performance-card b,.performance-card span{display:block}.performance-card b{color:var(--blue);font-size:18px}.performance-card span{color:var(--muted);font-size:11px}.recommendation-card[data-player-id],.pitch-player[data-player-id]{cursor:pointer}[role="button"]:focus-visible{outline:2px solid var(--accent);outline-offset:2px}.breakdown-table{border:1px solid var(--line);border-radius:9px;overflow-x:auto;margin-top:4px}.breakdown-head,.breakdown-row{display:grid;grid-template-columns:60px repeat(8,minmax(72px,1fr));gap:8px;align-items:center;min-width:760px;padding:9px 12px}.breakdown-head{background:var(--panel2);color:var(--muted);font-size:10px;text-transform:uppercase;letter-spacing:.05em}.breakdown-row{border-top:1px solid var(--line)}.breakdown-row .total{color:var(--accent);font-weight:800}.breakdown-note{margin-top:8px;color:var(--muted);font-size:12px}
#decision-bench-panel,#decision-model-panel{grid-column:1/-1}.model-summary-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.model-disclosure{margin-top:8px;border-top:1px solid var(--line);padding-top:10px}.model-disclosure summary{min-height:40px;display:flex;align-items:center;color:var(--blue);cursor:pointer;font-weight:700}.model-detail-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:0 18px}
.breakdown-head,.breakdown-row{grid-template-columns:54px repeat(11,minmax(70px,1fr))}
@media(max-width:1080px){.app{grid-template-columns:210px minmax(0,1fr)}.content{padding:24px 22px}.metrics{grid-template-columns:repeat(2,1fr)}.metric,.metric:first-child,.metric:last-child{border-radius:9px}.toolbar{grid-template-columns:1.3fr 1fr 1fr}.player-toolbar{grid-template-columns:repeat(2,1fr)}.decision-summary{grid-template-columns:repeat(3,1fr)}.recommendation-grid.compact{grid-template-columns:repeat(3,1fr)}.profile-comparison{grid-template-columns:repeat(3,1fr)}.chip-alternatives{grid-template-columns:repeat(2,1fr)}}
@media(max-width:760px){.app{display:block}.sidebar{display:none}.mobile-nav-wrap{display:block;margin-bottom:14px}.mobile-nav-wrap label{display:block;color:var(--muted);font-size:11px;text-transform:uppercase;letter-spacing:.06em}.mobile-nav-wrap select{width:100%;min-height:44px;margin-top:4px;background:var(--panel2);border:1px solid var(--line);color:var(--text);border-radius:8px;padding:8px}.content{padding:18px 14px}.topbar{flex-direction:column}.header-actions{justify-content:flex-start;width:100%;max-width:none}.refresh-message,.refresh-source-status{text-align:left}.attention-item{grid-template-columns:auto 1fr}.attention-action{grid-column:2;text-align:left}.metrics,.grid,.transfer-layout,.toolbar,.more-grid,.team-summary,.squad-grid,.player-toolbar,.fixture-toolbar,.decision-layout,.decision-summary,.recommendation-grid,.recommendation-grid.compact,.profile-options,.profile-comparison,.scenario-options,.weekly-plan-summary,.chip-alternatives,.model-summary-grid,.model-detail-grid{grid-template-columns:1fr}.weekly-bench{grid-template-columns:repeat(2,minmax(0,1fr))}.formation-pitch{min-height:440px;padding:22px 7px;gap:10px}.pitch-row{gap:4px}.pitch-player{width:19.2%;padding:7px 3px}.pitch-player strong{font-size:10px}.pitch-player span{font-size:9px}.fixture-row{grid-template-columns:1fr;gap:8px}.fixture-team.away{flex-direction:row}.player-table{overflow-x:auto}.player-head,.player-row{min-width:720px}.club-grid{grid-template-columns:1fr}.transfer{grid-template-columns:1fr}.inspector{position:static;order:-1}.filter-shell{top:0}.filter-meta{align-items:flex-start;flex-direction:column}.topbar h1{font-size:24px}}
@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important}}
</style>
</head>
<body><div class="app">
<aside class="sidebar"><div class="brand"><strong>FPL Intelligence</strong><span>2026/27 workspace</span></div><nav class="nav" aria-label="Workspace navigation">
<button class="active" aria-current="page" data-view="overview">Preseason overview</button>
<button data-view="decisions">Decision Center</button><button data-view="squad">My Team</button><button data-view="players">Player Explorer</button><button data-view="fixtures">Fixtures</button><button data-view="transfers">Transfers &amp; News</button><button data-view="performance">Model Performance</button><button data-view="model">Model Status</button>
</nav></aside>
<main class="content">
<div class="mobile-nav-wrap"><label for="mobile-nav">Section</label><select id="mobile-nav"><option value="overview">Preseason overview</option><option value="decisions">Decision Center</option><option value="squad">My Team</option><option value="players">Player Explorer</option><option value="fixtures">Fixtures</option><option value="transfers">Transfers &amp; News</option><option value="performance">Model Performance</option><option value="model">Model Status</option></select></div>
<header class="topbar"><div><div class="eyebrow">Decision workspace</div><h1 id="view-title">Preseason overview</h1><div class="muted">Top-50k objective · balanced risk · Eastern Time (New York) · free first-party data</div><div id="deadline-status" class="deadline">Deadline appears when the 2026/27 feed launches.</div></div><div class="header-actions"><button id="refresh-now" class="refresh-button" type="button">Refresh now</button><div class="pill" id="season-pill">Checking feed</div><div id="refresh-message" class="refresh-message" role="status" aria-live="polite"></div><div id="refresh-source-status" class="refresh-source-status" aria-live="polite"></div></div></header>
<section id="view-overview" class="view active">
<section class="attention-panel"><div class="section-heading"><h2>What needs attention</h2><span class="muted">Only material items</span></div><div id="attention" class="attention"></div></section>
<div class="metrics" aria-label="Current workspace status"><div class="metric primary"><b id="season-readiness">Waiting for 2026/27 FPL launch</b><span>season readiness</span></div><div class="metric"><b id="new-count">0</b><span>relevant changes this week</span></div><div class="metric"><b id="high-count">0</b><span>known FPL players affected</span></div><div class="metric"><b id="pending-count">0</b><span>arrivals awaiting reconciliation</span></div></div>
<div class="grid"><div class="overview-stack">
<section class="panel"><h2>Since last refresh</h2><div id="changes" class="change-summary"></div></section>
<section id="preseason-workflow" class="panel"><h2>Preseason workflow</h2><div class="steps"><div class="step"><b>1. Confirm movements</b><div class="muted">First-party announcements only.</div></div><div class="step"><b>2. Reconcile with FPL</b><div class="muted">Current prices, positions, clubs, and player IDs are loaded.</div></div><div class="step"><b>3. Estimate roles</b><div class="muted">Preliminary minutes use official prior-season starts and availability.</div></div><div class="step"><b>4. Compare legal drafts</b><div class="muted">Three five-gameweek risk-profile squad recommendations are in Decision Center.</div></div></div></section>
</div><div class="overview-stack">
<section class="panel"><h2>Data freshness</h2><dl id="freshness"></dl></section>
<section class="panel"><h2>Club movement summary</h2><div class="muted" style="margin-bottom:12px">Select a club to inspect its relevant movements.</div><div id="club-summary" class="club-grid"></div></section>
</div></div></section>
<section id="view-transfers" class="view"><section class="panel"><div class="eyebrow">Secondary evidence view</div><h2 style="font-size:19px;margin-top:3px">Confirmed transfers and squad changes</h2><div class="muted">Recent, potentially relevant records are shown by default. Every confirmed record remains retrievable.</div>
<div class="filter-shell"><div class="toolbar"><div class="field"><label for="transfer-search">Search</label><input id="transfer-search" type="search" placeholder="Player or club"></div><div class="field"><label for="club-filter">Club</label><select id="club-filter"><option value="all">All clubs</option></select></div><div class="field"><label for="relevance-filter">FPL relevance</label><select id="relevance-filter"><option value="actionable" selected>High + medium</option><option value="high">High only</option><option value="medium">Medium only</option><option value="low">Low only</option><option value="all">All records</option></select></div></div>
<details id="more-filters" class="more-filters"><summary>More filters</summary><div class="more-grid"><div class="field"><label for="direction-filter">Direction</label><select id="direction-filter"><option value="all">In and out</option><option value="in">Incoming</option><option value="out">Outgoing</option><option value="released">Released</option></select></div><div class="field"><label for="movement-filter">Movement</label><select id="movement-filter"><option value="all">All types</option><option value="transfer-in">Transfer in</option><option value="transfer-out">Transfer out</option><option value="loan-in">Loan in</option><option value="loan-out">Loan out</option><option value="player-released">Released</option><option value="end-of-loan">End of loan</option></select></div><div class="field"><label for="freshness-filter">Date</label><select id="freshness-filter"><option value="recent14" selected>Last 14 days</option><option value="new_7d">Last 7 days</option><option value="older">Older</option><option value="all">All dates</option></select></div></div></details>
<div class="filter-meta"><div id="active-filters" class="active-filters" aria-label="Active filters"></div><button id="reset-filters" class="reset-filters" type="button">Reset filters</button></div></div>
<div class="countline"><span id="result-count">0 results</span><span>20 per page</span></div><div class="transfer-layout"><div><div id="feed" class="feed"><div class="empty">No confirmed transfers have been loaded yet. This does not mean no transfers have occurred. Refresh from first-party sources.</div></div><div class="pagination"><button id="prev-page">Previous</button><span id="page-label">Page 1 of 1</span><button id="next-page">Next</button></div></div><aside class="panel inspector"><h2>Evidence inspector</h2><div id="inspector" class="empty" aria-live="polite">Select a result to inspect its source, classification, and FPL reconciliation state.</div></aside></div>
</section></section>
<section id="view-decisions" class="view"><section class="panel decision-hero"><div class="section-heading"><div><div class="eyebrow" id="decision-eyebrow">Preliminary recommendation</div><h2 id="decision-heading" style="font-size:20px;margin-top:3px">Fresh-squad benchmark</h2></div><span class="status-good" id="decision-status">Model checking inputs</span></div><div id="decision-summary" class="decision-summary"></div><p class="muted" id="decision-context">This is a reproducible projection baseline, not a guarantee.</p><aside id="fixture-congestion-limitation" class="limitation-note" role="note"><strong>Fixture-congestion limitation</strong>Projections use official Premier League fixtures and FPL fixture difficulty. European and domestic-cup schedules are not yet modeled directly, so their travel, fatigue, and rotation effects are not included explicitly.</aside></section><section class="panel weekly-panel" style="margin-top:14px"><div class="section-heading"><div><div class="eyebrow">Personalized weekly decision</div><h2 id="weekly-heading">Roll, transfer, and chip recommendation</h2></div><span id="weekly-status" class="status-wait">Checking published squad</span></div><div id="weekly-profile-options" class="profile-options" role="tablist" aria-label="Weekly decision risk profiles"></div><div id="weekly-profile-panel" role="tabpanel" tabindex="0"><div id="weekly-summary" class="decision-summary"></div><div id="weekly-recommendation" class="weekly-recommendation"></div><div id="weekly-scenarios" class="scenario-options"></div><section id="weekly-plan" class="weekly-plan" hidden><div class="section-heading"><div><h3>Five-gameweek planner</h3><span class="muted">Only the next action is recommended</span></div><span id="weekly-plan-confidence" class="status-wait"></span></div><div id="weekly-plan-summary" class="weekly-plan-summary"></div><h3 style="margin-top:13px">Conditional future branches</h3><div id="weekly-branches" class="conditional-branches"></div><details class="planner-assumptions"><summary>Planner assumptions and limitations</summary><div id="weekly-plan-assumptions"></div></details></section><section id="weekly-lineup" class="weekly-lineup"><div class="section-heading"><h3 id="weekly-lineup-heading">Recommended post-decision XI</h3><span id="weekly-lineup-meta" class="muted"></span></div><div id="weekly-pitch" class="formation-pitch" role="group" aria-label="Recommended post-decision team formation"></div><div id="weekly-bench" class="weekly-bench" aria-label="Recommended bench order"></div></section><div id="weekly-chip" class="weekly-chip"></div><div id="weekly-rules" class="decision-note"></div></div></section><section class="panel profile-panel" style="margin-top:14px"><div class="section-heading"><div><h2>Compare risk profiles</h2><span class="muted">Select a team to update the XI, bench, captaincy, and full squad below.</span></div><span class="muted">Balanced is the default</span></div><div id="profile-options" class="profile-options" role="tablist" aria-label="Recommendation risk profiles"></div><div id="profile-panel" role="tabpanel" tabindex="0"><div id="profile-comparison" class="profile-comparison" aria-live="polite"></div></div></section><div class="decision-layout"><section class="panel"><div class="section-heading"><h2 id="recommended-xi-heading">Recommended XI</h2><span id="recommended-formation" class="muted"></span></div><div id="recommended-xi" class="formation-pitch" role="group" aria-label="Recommended team formation"></div></section><section class="panel"><h2>Captaincy</h2><div id="captaincy-list" class="decision-list"></div></section></div><div class="decision-layout" style="margin-top:14px"><section id="decision-bench-panel" class="panel"><h2>Bench order</h2><div id="recommended-bench" class="recommendation-grid compact"></div></section><section id="decision-model-panel" class="panel"><h2>Model basis and risks</h2><div id="decision-model" class="decision-list"></div></section></div><section class="panel" style="margin-top:14px"><div class="section-heading"><h2>Full recommended squad</h2><span class="muted">£100.0m maximum · 2 GKP · 5 DEF · 5 MID · 3 FWD · max 3 per club</span></div><div id="recommended-squad" class="recommendation-grid compact"></div></section><section class="panel" style="margin-top:14px"><div class="section-heading"><div><h2>Player scoring breakdown</h2><span class="muted" id="player-breakdown-name">Select a player above to inspect its projection</span></div></div><div id="player-breakdown-table"></div></section></section>
<section id="view-squad" class="view"><div class="grid"><section class="panel"><div class="eyebrow">Public manager connection</div><h2>My Team</h2><div id="my-team-summary" class="team-summary"></div></section><section class="panel"><h2>Connection status</h2><div id="manager-status" class="empty">Checking public manager data.</div></section></div><section class="panel" style="margin-top:14px"><div class="section-heading"><h2>Latest public squad</h2><span class="muted">Official FPL</span></div><div id="squad-grid" class="squad-grid"></div></section></section>
<section id="view-players" class="view"><section class="panel"><div class="section-heading"><div><div class="eyebrow">Official 2026/27 data</div><h2 style="font-size:19px;margin-top:3px">Player Explorer</h2></div><span class="status-good">Official FPL prices</span></div><p class="muted">Search current prices, positions, clubs, ownership, and official availability. Preliminary modeled projections and recommendations are separated in Decision Center.</p><div class="player-toolbar"><div class="field"><label for="player-search">Search</label><input id="player-search" type="search" placeholder="Player name"></div><div class="field"><label for="player-club-filter">Club</label><select id="player-club-filter"><option value="all">All clubs</option></select></div><div class="field"><label for="player-position-filter">Position</label><select id="player-position-filter"><option value="all">All positions</option></select></div><div class="field"><label for="player-sort">Sort</label><select id="player-sort"><option value="price-desc">Price: high to low</option><option value="price-asc">Price: low to high</option><option value="ownership-desc">Ownership: high to low</option><option value="name">Name</option></select></div></div><div class="countline"><span id="player-count">0 players</span><span>30 per page</span></div><div style="overflow-x:auto"><table class="player-table"><thead><tr class="player-head"><th scope="col">Player</th><th scope="col">Club</th><th scope="col">Position</th><th scope="col">Price</th><th scope="col">Owned</th><th scope="col">Status</th></tr></thead><tbody id="player-results"></tbody></table></div><div class="pagination"><button id="player-prev" type="button">Previous</button><span id="player-page">Page 1 of 1</span><button id="player-next" type="button">Next</button></div></section></section>
<section id="view-fixtures" class="view"><section class="panel"><div class="section-heading"><div><div class="eyebrow">Official 2026/27 schedule</div><h2 style="font-size:19px;margin-top:3px">Fixtures</h2></div><span class="status-good">Official FPL fixtures</span></div><p class="muted">Kickoff times and official Fixture Difficulty Ratings. Difficulty runs from 1 (easier) to 5 (harder); it is not a points projection.</p><div class="fixture-toolbar"><div class="field"><label for="fixture-gameweek">Gameweek</label><select id="fixture-gameweek"></select></div><div class="field"><label for="fixture-club-filter">Club</label><select id="fixture-club-filter"><option value="all">All clubs</option></select></div></div><div class="countline"><span id="fixture-count">0 fixtures</span><span>Difficulty: 1 easier · 5 harder</span></div><div id="fixture-results" class="fixture-list"></div></section></section>
<section id="view-performance" class="view"><section class="panel"><div class="section-heading"><div><div class="eyebrow">Forecast accountability</div><h2 style="font-size:19px;margin-top:3px">Modeled vs actual points</h2></div><span id="performance-status" class="status-wait">Waiting for completed Gameweeks</span></div><p class="muted">Pre-event forecasts are frozen, then compared with official FPL points after each Gameweek finishes. Results are never backfilled with hindsight lineups.</p><div id="performance-summary" class="decision-summary"></div><div id="performance-errors" role="status" aria-live="polite"></div></section><div class="grid" style="margin-top:14px"><section class="panel"><h2>Accuracy by horizon</h2><div id="performance-horizons" class="performance-horizons"></div></section><section class="panel"><h2>Calibration diagnostics</h2><div id="performance-calibration"></div></section></div><section class="panel" style="margin-top:14px"><div class="section-heading"><h2>Forecast history</h2><span class="muted">Official actual points</span></div><div style="overflow-x:auto"><table class="performance-table"><thead><tr class="performance-head"><th scope="col">Origin</th><th scope="col">Profile</th><th scope="col">Horizon</th><th scope="col">Modeled</th><th scope="col">Official actual</th><th scope="col">Error</th><th scope="col">In range</th></tr></thead><tbody id="performance-history"></tbody></table></div><p id="performance-method" class="muted" style="margin-bottom:0"></p></section></section>
<section id="view-model" class="view"><div class="grid"><section class="panel"><h2>Feed readiness</h2><dl id="model-readiness"></dl></section><section class="panel"><h2>Source registry</h2><div id="source-list" class="source-list"></div></section><section class="panel"><h2>Projection policy</h2><p>Official FPL is canonical for prices, positions, IDs, fixtures, and scoring. First-party Premier League and club announcements confirm transfers. No betting odds are used.</p></section><section class="panel"><h2>Account boundary</h2><p>No password is stored and no account action is performed automatically.</p></section></div></section>
<footer>Recommendations only. Evidence is locally stored with source provenance.</footer>
</main></div>
<script id="dashboard-data" type="application/json">__DASHBOARD_DATA__</script>
<script>
const state=JSON.parse(document.getElementById('dashboard-data').textContent);const transfers=state.transfers||[];const players=state.players||[];const fixtures=state.fixtures||[];const decision=state.decision_center||{status:'model_unavailable'};const performance=state.model_performance||{status:'waiting_for_results',comparisons:[],summary:{},by_horizon:{},calibration:{recommendations:[]}};const byId=id=>document.getElementById(id);const esc=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));const pageSize=20;let page=1;let playerPage=1;let selectedBreakdownPlayerId=null;
const trustedLinkDomains=new Set(__TRUSTED_LINK_DOMAINS__);
function safeLink(url,label){try{const parsed=new URL(url);const host=parsed.hostname.toLowerCase().replace(/^www\./,'');if(parsed.protocol==='https:'&&trustedLinkDomains.has(host))return `<a href="${esc(parsed.href)}" target="_blank" rel="noopener noreferrer">${esc(label)}</a>`;}catch(error){}return `<span>${esc(label)} · invalid or untrusted source URL</span>`;}
const titles={overview:state.fpl.season_phase==='in_season'?'Season overview':'Preseason overview',decisions:'Decision Center',squad:'My Team',players:'Player Explorer',fixtures:'Fixtures',transfers:'Transfers & News',performance:'Model Performance',model:'Model Status'};
const seasonLabel=()=>state.fpl.ready_for_2026_27?(state.fpl.season_phase==='in_season'?'2026/27 season active':'2026/27 FPL feed ready'):'Waiting for 2026/27 FPL launch';const timezoneLabel=state.timezone==='America/New_York'?'Eastern Time (New York)':state.timezone;
function showView(name){document.querySelectorAll('.view').forEach(node=>node.classList.toggle('active',node.id===`view-${name}`));document.querySelectorAll('[data-view]').forEach(node=>{const active=node.dataset.view===name;node.classList.toggle('active',active);active?node.setAttribute('aria-current','page'):node.removeAttribute('aria-current');});byId('mobile-nav').value=name;byId('view-title').textContent=titles[name]||'FPL Intelligence';if(name==='transfers')applyFilters();window.scrollTo({top:0,behavior:'smooth'});}
document.querySelectorAll('[data-view]').forEach(button=>button.addEventListener('click',()=>showView(button.dataset.view)));byId('mobile-nav').addEventListener('change',event=>showView(event.target.value));
function fmtDate(value,withTime=false){if(!value)return 'Not recorded';const options=withTime?{month:'short',day:'numeric',hour:'numeric',minute:'2-digit',timeZone:'America/New_York',timeZoneName:'short'}:{month:'short',day:'numeric',year:'numeric',timeZone:'America/New_York'};return new Intl.DateTimeFormat('en-US',options).format(new Date(value));}
function deadlineText(){if(!state.fpl.next_deadline)return 'Deadline appears when the 2026/27 feed launches.';const deadline=new Date(state.fpl.next_deadline);if(deadline <= Date.now())return `${state.fpl.next_event_name||'Deadline'} · ${fmtDate(state.fpl.next_deadline,true)} · Deadline passed. Refresh required to load the next deadline.`;const hours=Math.floor((deadline-Date.now())/3600000);const days=Math.floor(hours/24);const remainder=hours%24;return `${state.fpl.next_event_name||'Next deadline'} · ${fmtDate(state.fpl.next_deadline,true)} · ${days}d ${remainder}h remaining`;}
function renderOverview(){const summary=state.transfer_summary||{total:transfers.length,high:0,medium:0,low:0,actionable:0};const relevantNew=transfers.filter(row=>['high','medium'].includes(row.fpl_relevance)&&row.freshness==='new_7d').length;byId('season-readiness').textContent=seasonLabel();byId('new-count').textContent=relevantNew;byId('high-count').textContent=summary.high||0;byId('pending-count').textContent=summary.medium||0;byId('season-pill').textContent=state.fpl.ready_for_2026_27?'Feed ready':'Feed pending';byId('deadline-status').textContent=deadlineText();if(state.fpl.season_phase==='in_season')byId('preseason-workflow').hidden=true;
const attention=[];if(!state.fpl.ready_for_2026_27)attention.push({level:'Monitor',kind:'info',title:'2026/27 FPL feed is not available yet',body:'Prices, positions, fixtures, and draft optimization remain locked.',action:'Review model status',view:'model'});else if(decision.status==='active_preliminary')attention.push({level:'Ready',kind:'ready',title:'Preliminary GW1 recommendation is available',body:'Official player and fixture data now powers a legal five-gameweek opening-squad baseline.',action:'Open Decision Center',view:'decisions'});else attention.push({level:'Review',kind:'info',title:'Target-season feed verified',body:'Official data is ready, but the recommendation model does not yet have complete inputs.',action:'Review model status',view:'model'});if(relevantNew)attention.push({level:'Review',kind:'',title:`${relevantNew} relevant changes this week`,body:'Review affected clubs and potential role changes before updating a draft.',action:'Open transfers',view:'transfers'});if(!relevantNew&&state.fpl.ready_for_2026_27)attention.push({level:'Info',kind:'info',title:'No material transfer changes this week',body:'No immediate transfer-news review is required.'});byId('attention').innerHTML=attention.map(item=>`<div class="attention-item"><span class="severity ${item.kind}">${esc(item.level)}</span><div><strong>${esc(item.title)}</strong><span class="muted">${esc(item.body)}</span></div>${item.view?`<button class="attention-action" data-go="${item.view}">${esc(item.action)}</button>`:''}</div>`).join('');document.querySelectorAll('[data-go]').forEach(button=>button.addEventListener('click',()=>showView(button.dataset.go)));
byId('freshness').innerHTML=`<dt>Last refreshed</dt><dd>${esc(fmtDate(state.generated_at,true))}</dd><dt>Timezone</dt><dd>${esc(timezoneLabel)}</dd><dt>FPL feed</dt><dd class="${state.fpl.ready_for_2026_27?'status-good':'status-wait'}">${esc(seasonLabel())}</dd><dt>Official records</dt><dd>${summary.total||0}</dd>`;renderChanges();
const clubs=(state.club_summaries||[]).filter(item=>item.relevant_moves>0).slice(0,10);byId('club-summary').innerHTML=clubs.length?clubs.map(item=>`<button class="club-card" data-club="${esc(item.club)}"><strong>${esc(item.club)}</strong><small>${item.arrivals} in · ${item.departures} out · ${item.relevant_moves} relevant</small></button>`).join(''):'<div class="empty">No relevant club movements are available yet.</div>';document.querySelectorAll('.club-card').forEach(button=>button.addEventListener('click',()=>{byId('club-filter').value=button.dataset.club;page=1;showView('transfers');}));}
function renderChanges(){const changes=state.changes_since_last_refresh||{};if(!changes.has_previous_snapshot){byId('changes').innerHTML='<div class="muted">Baseline snapshot recorded. Material changes will appear after the next refresh.</div>';return;}const rows=[];if(changes.new_confirmed_transfers)rows.push(['New confirmed movements',`+${changes.new_confirmed_transfers}`]);if(changes.new_fpl_players)rows.push(['New FPL players',`+${changes.new_fpl_players}`]);if(changes.club_mapping_changes)rows.push(['Club mappings changed',changes.club_mapping_changes]);if(changes.availability_changes)rows.push(['Availability changes',changes.availability_changes]);byId('changes').innerHTML=rows.length?rows.map(row=>`<div class="change-row"><span>${esc(row[0])}</span><span class="change-value">${esc(row[1])}</span></div>`).join(''):'<div class="status-good">No material changes since your last refresh.</div>';}
function populateFilters(){const clubs=(state.club_summaries||[]).map(item=>item.club).filter(Boolean).sort((a,b)=>a.localeCompare(b));byId('club-filter').insertAdjacentHTML('beforeend',clubs.map(club=>`<option value="${esc(club)}">${esc(club)}</option>`).join(''));}
function filteredRows(){const query=byId('transfer-search').value.trim().toLocaleLowerCase();const club=byId('club-filter').value;const relevance=byId('relevance-filter').value;const direction=byId('direction-filter').value;const movement=byId('movement-filter').value;const freshness=byId('freshness-filter').value;return transfers.filter(row=>{const text=[row.player,row.from_club,row.to_club,row.premier_league_club].join(' ').toLocaleLowerCase();const relevanceOk=relevance==='all'||relevance==='actionable'&&['high','medium'].includes(row.fpl_relevance)||row.fpl_relevance===relevance;const freshOk=freshness==='all'||freshness==='recent14'&&['new_7d','recent_14d'].includes(row.freshness)||row.freshness===freshness;return(!query||text.includes(query))&&(club==='all'||row.premier_league_club===club)&&relevanceOk&&(direction==='all'||row.movement_direction===direction)&&(movement==='all'||row.movement_type===movement)&&freshOk;});}
const readable={pending_new_season_fpl:'Awaiting 2026/27 FPL match',matched_current_fpl:'Matched to current 2026/27 FPL player',matched_prior_fpl:'Matched to prior FPL player',confirmed_first_party:'First-party confirmed','transfer-in':'Transfer in','transfer-out':'Transfer out','loan-in':'Loan in','loan-out':'Loan out','player-released':'Released','end-of-loan':'End of loan'};const label=value=>readable[value]||String(value||'Not recorded').replaceAll('_',' ');
function whyMatters(row){if(row.matched_fpl_element_id)return 'Known FPL player changed club or squad context.';if(row.movement_direction==='in')return 'Confirmed arrival may affect roles and starting minutes.';if(row.movement_direction==='out')return 'Departure may change minutes for the remaining squad.';return 'Retained as verified squad evidence.';}
function renderFilterChips(){const values=[['relevance-filter',byId('relevance-filter').selectedOptions[0].text],['freshness-filter',byId('freshness-filter').selectedOptions[0].text]];if(byId('transfer-search').value)values.unshift(['transfer-search',`Search: ${byId('transfer-search').value}`]);if(byId('club-filter').value!=='all')values.push(['club-filter',byId('club-filter').value]);if(byId('direction-filter').value!=='all')values.push(['direction-filter',byId('direction-filter').selectedOptions[0].text]);if(byId('movement-filter').value!=='all')values.push(['movement-filter',byId('movement-filter').selectedOptions[0].text]);byId('active-filters').innerHTML=values.map(item=>`<button class="filter-chip" data-clear="${item[0]}" type="button">${esc(item[1])} ×</button>`).join('');document.querySelectorAll('[data-clear]').forEach(button=>button.addEventListener('click',()=>{const id=button.dataset.clear;byId(id).value=id==='transfer-search'?'':'all';page=1;applyFilters();}));}
function applyFilters(){const rows=filteredRows();const pages=Math.max(1,Math.ceil(rows.length/pageSize));page=Math.min(page,pages);const visible=rows.slice((page-1)*pageSize,page*pageSize);byId('result-count').textContent=`${rows.length} result${rows.length===1?'':'s'}`;byId('page-label').textContent=`Page ${page} of ${pages}`;byId('prev-page').disabled=page<=1;byId('next-page').disabled=page>=pages;renderFilterChips();const feed=byId('feed');if(!visible.length){feed.innerHTML='<div class="empty">No confirmed transfers match these filters. Broaden the date or relevance setting.</div>';return;}feed.innerHTML=visible.map(row=>`<button class="transfer"><span><strong>${esc(row.player)}</strong><small>${esc(row.from_club)} → ${esc(row.to_club)} · ${esc(fmtDate(row.announced_at))}</small><span class="why">${esc(whyMatters(row))}</span><span class="tags"><span class="tag ${esc(row.fpl_relevance)}">${esc(label(row.fpl_relevance))} relevance</span><span class="tag">${esc(label(row.movement_type))}</span><span class="tag">${esc(label(row.fpl_reconciliation_status))}</span></span></span><span class="muted">${esc(row.premier_league_club)}</span></button>`).join('');feed.querySelectorAll('.transfer').forEach((button,index)=>button.addEventListener('click',()=>inspect(visible[index])));}
function inspect(row){const links=(row.supporting_source_urls||[row.source_url]).filter(Boolean).map((url,index)=>safeLink(url,`Source ${index+1}`)).join(' · ');byId('inspector').className='';byId('inspector').innerHTML=`<dl><dt>Player</dt><dd>${esc(row.player)}</dd><dt>Movement</dt><dd>${esc(row.from_club)} → ${esc(row.to_club)}</dd><dt>Why it matters</dt><dd>${esc(whyMatters(row))}</dd><dt>PL club</dt><dd>${esc(row.premier_league_club)}</dd><dt>Announced</dt><dd>${esc(fmtDate(row.announced_at,true))}</dd><dt>FPL relevance</dt><dd>${esc(label(row.fpl_relevance))}</dd><dt>Verification</dt><dd>${esc(label(row.verification_status))}</dd><dt>FPL status</dt><dd>${esc(label(row.fpl_reconciliation_status))}</dd><dt>Evidence</dt><dd>${links}</dd></dl>`;}
const filterIds=['transfer-search','club-filter','relevance-filter','direction-filter','movement-filter','freshness-filter'];filterIds.forEach(id=>byId(id).addEventListener(id==='transfer-search'?'input':'change',()=>{page=1;applyFilters();}));byId('reset-filters').addEventListener('click',()=>{byId('transfer-search').value='';byId('club-filter').value='all';byId('relevance-filter').value='actionable';byId('direction-filter').value='all';byId('movement-filter').value='all';byId('freshness-filter').value='recent14';page=1;applyFilters();});byId('prev-page').addEventListener('click',()=>{page=Math.max(1,page-1);applyFilters();});byId('next-page').addEventListener('click',()=>{page+=1;applyFilters();});
function setupPlayerExplorer(){const clubs=[...new Set(players.map(player=>player.club).filter(Boolean))].sort((a,b)=>a.localeCompare(b));const positions=[...new Set(players.map(player=>player.position).filter(Boolean))].sort((a,b)=>a.localeCompare(b));byId('player-club-filter').insertAdjacentHTML('beforeend',clubs.map(club=>`<option value="${esc(club)}">${esc(club)}</option>`).join(''));byId('player-position-filter').insertAdjacentHTML('beforeend',positions.map(position=>`<option value="${esc(position)}">${esc(position)}</option>`).join(''));['player-search','player-club-filter','player-position-filter','player-sort'].forEach(id=>byId(id).addEventListener(id==='player-search'?'input':'change',()=>{playerPage=1;renderPlayers();}));byId('player-prev').addEventListener('click',()=>{playerPage=Math.max(1,playerPage-1);renderPlayers();});byId('player-next').addEventListener('click',()=>{playerPage+=1;renderPlayers();});renderPlayers();}
function renderPlayers(){const query=byId('player-search').value.trim().toLocaleLowerCase();const club=byId('player-club-filter').value;const position=byId('player-position-filter').value;const sort=byId('player-sort').value;const rows=players.filter(player=>(!query||`${player.name} ${player.full_name||''}`.toLocaleLowerCase().includes(query))&&(club==='all'||player.club===club)&&(position==='all'||player.position===position));const comparisons={name:(a,b)=>(a.name||'').localeCompare(b.name||''),'price-desc':(a,b)=>b.price-a.price||(a.name||'').localeCompare(b.name||''),'price-asc':(a,b)=>a.price-b.price||(a.name||'').localeCompare(b.name||''),'ownership-desc':(a,b)=>b.ownership-a.ownership||(a.name||'').localeCompare(b.name||'')};rows.sort(comparisons[sort]||comparisons['price-desc']);const pages=Math.max(1,Math.ceil(rows.length/30));playerPage=Math.min(playerPage,pages);const visible=rows.slice((playerPage-1)*30,playerPage*30);byId('player-count').textContent=`${rows.length} player${rows.length===1?'':'s'}`;byId('player-page').textContent=`Page ${playerPage} of ${pages}`;byId('player-prev').disabled=playerPage<=1;byId('player-next').disabled=playerPage>=pages;const statuses={a:'Available',d:'Doubtful',i:'Injured',s:'Suspended',u:'Unavailable',n:'Not available'};byId('player-results').innerHTML=visible.length?visible.map(player=>`<tr class="player-row"><th scope="row"><strong>${esc(player.name)}</strong><small>${esc(player.full_name||'')}</small></th><td>${esc(player.club)}</td><td>${esc(player.position)}</td><td class="price">£${Number(player.price).toFixed(1)}m</td><td>${Number(player.ownership).toFixed(1)}%</td><td><strong>${esc(statuses[player.status]||player.status||'Unknown')}</strong>${player.news?`<small>${esc(player.news)}</small>`:''}</td></tr>`).join(''):'<tr><td colspan="6"><div class="empty">No players match these filters.</div></td></tr>';}
function setupFixtures(){const events=[...new Set(fixtures.map(row=>row.event).filter(Boolean))].sort((a,b)=>a-b);byId('fixture-gameweek').innerHTML=events.length?events.map(event=>`<option value="${event}">Gameweek ${event}</option>`).join(''):'<option value="">No gameweeks</option>';const preferred=String(state.fpl.next_event_id||events[0]||'');if(events.map(String).includes(preferred))byId('fixture-gameweek').value=preferred;const clubs=[...new Set(fixtures.flatMap(row=>[row.home_team,row.away_team]).filter(Boolean))].sort((a,b)=>a.localeCompare(b));byId('fixture-club-filter').insertAdjacentHTML('beforeend',clubs.map(club=>`<option value="${esc(club)}">${esc(club)}</option>`).join(''));byId('fixture-gameweek').addEventListener('change',renderFixtures);byId('fixture-club-filter').addEventListener('change',renderFixtures);renderFixtures();}
function renderFixtures(){const event=Number(byId('fixture-gameweek').value);const club=byId('fixture-club-filter').value;const rows=fixtures.filter(row=>(!event||row.event===event)&&(club==='all'||row.home_team===club||row.away_team===club)).sort((a,b)=>String(a.kickoff_time||'').localeCompare(String(b.kickoff_time||'')));byId('fixture-count').textContent=`${rows.length} fixture${rows.length===1?'':'s'}`;byId('fixture-results').innerHTML=rows.length?rows.map(row=>{const result=row.finished?`${row.home_score} – ${row.away_score}`:'vs';return `<div class="fixture-row"><span class="muted">${esc(fmtDate(row.kickoff_time,true))}</span><span class="fixture-team"><strong>${esc(row.home_team)}</strong><span class="difficulty d${esc(row.home_difficulty)}" title="Home difficulty">${esc(row.home_difficulty)}</span></span><strong>${esc(result)}</strong><span class="fixture-team away"><strong>${esc(row.away_team)}</strong><span class="difficulty d${esc(row.away_difficulty)}" title="Away difficulty">${esc(row.away_difficulty)}</span></span></div>`;}).join(''):'<div class="empty">No fixtures match this gameweek and club.</div>';}
function renderDecisionLegacy(){const active=decision.status==='active_preliminary'&&decision.recommended_squad;if(!active){byId('decision-status').className='status-wait';byId('decision-status').textContent='Recommendation unavailable';byId('decision-summary').innerHTML=`<div class="empty">${esc(decision.reason||'Projection and optimization inputs are incomplete.')}</div>`;['recommended-xi','recommended-bench','captaincy-list','decision-model','recommended-squad'].forEach(id=>byId(id).innerHTML='<div class="empty">No modeled recommendation is available.</div>');return;}const squad=decision.recommended_squad;const captainId=squad.captain&&squad.captain.id;const viceId=squad.vice_captain&&squad.vice_captain.id;const card=(player,index=null)=>{const role=player.id===captainId?' (C)':player.id===viceId?' (VC)':'';const bench=index===null?'':`Bench ${index+1} · `;return `<div class="recommendation-card ${player.id===captainId?'captain':''}"><strong>${esc(player.name)}${role}</strong><span>${esc(player.position_short)} · ${esc(player.club)} · £${Number(player.price).toFixed(1)}m</span><span>${bench}${Number(player.expected_minutes).toFixed(0)} expected min · ${esc(player.confidence)} confidence</span><span class="projection">${Number(player.xp_1).toFixed(1)} / ${Number(player.xp_3).toFixed(1)} / ${Number(player.xp_5).toFixed(1)} xPts</span><span>5-GW range ${Number(player.lower_5).toFixed(1)}–${Number(player.upper_5).toFixed(1)}</span></div>`;};byId('decision-status').className='status-good';byId('decision-status').textContent='Active · preliminary model';byId('decision-summary').innerHTML=`<div class="decision-metric"><b>£${Number(squad.cost).toFixed(1)}m</b><span>Squad cost</span></div><div class="decision-metric"><b>£${Number(squad.money_remaining).toFixed(1)}m</b><span>Money remaining</span></div><div class="decision-metric"><b>${Number(squad.projected_gw1_points_including_captain).toFixed(1)}</b><span>Modeled GW1 points, captain included</span></div><div class="decision-metric"><b>${esc(squad.captain.name)}</b><span>Captain · ${Number(squad.captain.xp_1).toFixed(1)} xPts before doubling</span></div><div class="decision-metric"><b>${esc(squad.vice_captain.name)}</b><span>Vice-captain</span></div>`;byId('recommended-formation').textContent=`${esc(squad.formation)} · xPts shown for 1 / 3 / 5 GWs`;byId('recommended-xi').innerHTML=(squad.starting_xi||[]).map(player=>card(player)).join('');byId('recommended-bench').innerHTML=(squad.bench||[]).map((player,index)=>card(player,index)).join('');byId('recommended-squad').innerHTML=(squad.players||[]).map(player=>card(player)).join('');byId('captaincy-list').innerHTML=(decision.captaincy||[]).map((player,index)=>`<div class="decision-row"><span><strong>${index+1}. ${esc(player.name)}</strong><br><span class="muted">${esc(player.club)} · ${Number(player.expected_minutes).toFixed(0)} expected min</span></span><b>${Number(player.xp_1).toFixed(1)} xPts</b></div>`).join('');const model=decision.model||{};const inputs=(model.inputs||[]).map(item=>`<div class="decision-note"><strong>Input</strong><br>${esc(item)}</div>`).join('');const limits=(model.limitations||[]).map(item=>`<div class="decision-note"><strong>Risk</strong><br>${esc(item)}</div>`).join('');byId('decision-model').innerHTML=`<div class="decision-note"><strong>${esc(model.name||'Projection model')} v${esc(model.version||'')}</strong><br>No betting odds. Generated ${esc(fmtDate(decision.generated_at,true))}.</div>${inputs}${limits}`;}
const breakdownKeys=['appearance','attacking','clean_sheet','goals_conceded','defensive_contribution','saves','bonus','residual'];
function renderPlayerBreakdown(player){selectedBreakdownPlayerId=player.id;const modelNotes=[player.uses_team_strength?'fitted opponent model':null,player.uses_recency_minutes?'recency-weighted minutes':null].filter(Boolean).join(' · ');byId('player-breakdown-name').textContent=`${player.name} · ${player.position_short} · ${player.club}${modelNotes?` · ${modelNotes}`:''}`;const events=player.projection_events||[];const rows=player.component_xp||[];byId('player-breakdown-table').innerHTML=events.length&&rows.length?`<div class="breakdown-table"><div class="breakdown-head"><span>GW</span><span>Appear</span><span>Attack</span><span>Clean sheet</span><span>Conceded</span><span>Def. contrib.</span><span>Saves</span><span>Bonus</span><span>Residual</span><span>Modeled</span><span>ep_next adj.</span><span>Final xPts</span></div>${events.map((event,index)=>{const row=rows[index]||{};const modeled=Number(row.modeled_total_before_ep_next??breakdownKeys.reduce((sum,key)=>sum+Number(row[key]||0),0));const adjustment=Number(row.ep_next_adjustment||0);const blended=Number(row.blended_total??modeled+adjustment);return `<div class="breakdown-row"><span>GW${event}</span>${breakdownKeys.map(key=>`<span>${Number(row[key]||0).toFixed(2)}</span>`).join('')}<span>${modeled.toFixed(2)}</span><span>${adjustment.toFixed(2)}</span><span class="total">${blended.toFixed(2)}</span></div>`;}).join('')}</div><p class="breakdown-note">Each column is a named scoring component from official rate stats and fixture data, plus a shrunk over/under-performance residual. The separate official ep_next adjustment, when present, is disclosed rather than hidden inside these component totals.</p>`:'<div class="empty">No per-event breakdown is available for this player.</div>';}
function attachBreakdownHandlers(squadPlayers,preferredDefault){document.querySelectorAll('[data-player-id]').forEach(node=>{const id=Number(node.dataset.playerId);const player=(squadPlayers||[]).find(row=>row.id===id);if(!player)return;node.addEventListener('click',()=>renderPlayerBreakdown(player));node.addEventListener('keydown',event=>{if(event.key==='Enter'||event.key===' '){event.preventDefault();renderPlayerBreakdown(player);}});});const target=(squadPlayers||[]).find(row=>row.id===selectedBreakdownPlayerId)||preferredDefault||(squadPlayers||[])[0];if(target)renderPlayerBreakdown(target);}
function bindTabs(containerId,selector,activate){const container=byId(containerId);const tabs=[...container.querySelectorAll(selector)];tabs.forEach(tab=>{tab.addEventListener('click',()=>activate(tab));tab.addEventListener('keydown',event=>{if(!['ArrowLeft','ArrowRight','Home','End'].includes(event.key))return;event.preventDefault();const index=tabs.indexOf(tab);const targetIndex=event.key==='Home'?0:event.key==='End'?tabs.length-1:event.key==='ArrowRight'?(index+1)%tabs.length:(index-1+tabs.length)%tabs.length;const target=tabs[targetIndex];activate(target);const next=byId(target.id);if(next)next.focus();});});}
function renderDecision(profileId=null){
const profiles=decision.profile_recommendations||[];const legacy={id:'balanced',label:'Balanced',summary:'Central projection baseline',risk_note:'Preliminary model uncertainty.',objective:'Central five-gameweek projection',squad:decision.recommended_squad,captaincy:decision.captaincy||[],metrics:{}};const availableProfiles=profiles.length?profiles:[legacy];const selectedId=profileId||decision.default_profile||'balanced';const selected=availableProfiles.find(row=>row.id===selectedId)||availableProfiles[0];const active=decision.status==='active_preliminary'&&selected&&selected.squad;if(!active){byId('decision-status').className='status-wait';byId('decision-status').textContent='Recommendation unavailable';byId('decision-summary').innerHTML=`<div class="empty">${esc(decision.reason||'Projection and optimization inputs are incomplete.')}</div>`;byId('profile-options').innerHTML='';byId('profile-comparison').innerHTML='<div class="empty">No risk-profile comparison is available.</div>';['recommended-xi','recommended-bench','captaincy-list','decision-model','recommended-squad','player-breakdown-table'].forEach(id=>byId(id).innerHTML='<div class="empty">No modeled recommendation is available.</div>');byId('player-breakdown-name').textContent='Select a player above to inspect its projection';return;}
const squad=selected.squad;const metrics=selected.metrics||{};const captainId=squad.captain&&squad.captain.id;const viceId=squad.vice_captain&&squad.vice_captain.id;const card=(player,index=null)=>{const role=player.id===captainId?' (C)':player.id===viceId?' (VC)':'';const reserve=player.position_short==='GKP'&&index!==null?'Reserve goalkeeper · ':index===null?'':`Bench ${index+1} · `;return `<div class="recommendation-card ${player.id===captainId?'captain':''}" data-player-id="${player.id}" tabindex="0" role="button" aria-label="Inspect ${esc(player.name)}'s scoring breakdown"><strong>${esc(player.name)}${role}</strong><span>${esc(player.position_short)} · ${esc(player.club)} · £${Number(player.price).toFixed(1)}m</span><span>${reserve}${Number(player.expected_minutes).toFixed(0)} expected min · ${esc(player.confidence)} confidence</span><span class="projection">${Number(player.xp_1).toFixed(1)} / ${Number(player.xp_3).toFixed(1)} / ${Number(player.xp_5).toFixed(1)} xPts</span><span>5-GW range ${Number(player.lower_5).toFixed(1)}–${Number(player.upper_5).toFixed(1)}</span></div>`;};
const pitch=(lineup)=>['FWD','MID','DEF','GKP'].map(position=>{const row=(lineup||[]).filter(player=>player.position_short===position);return `<div class="pitch-row pitch-${position.toLowerCase()}">${row.map(player=>{const role=player.id===captainId?'C':player.id===viceId?'VC':'';return `<div class="pitch-player ${player.id===captainId?'captain':''}" data-player-id="${player.id}" tabindex="0" role="button" title="${esc(player.name)} · ${esc(player.club)} · ${Number(player.expected_minutes).toFixed(0)} expected minutes"><strong>${esc(player.name)}${role?` (${role})`:''}</strong><span>${esc(player.club)}</span><span class="projection">${Number(player.xp_1).toFixed(1)} / ${Number(player.xp_3).toFixed(1)} / ${Number(player.xp_5).toFixed(1)}</span></div>`;}).join('')}</div>`;}).join('');
byId('decision-eyebrow').textContent=`Preliminary GW${decision.event||1} benchmark`;byId('decision-heading').textContent=decision.event===1?'Opening-squad decision':'Fresh-squad benchmark';byId('decision-context').textContent=decision.event===1?'This is a reproducible preseason baseline, not a guarantee. Your unpublished draft is not inferred.':'This benchmark shows the best fresh squad for comparison; use the personalized weekly decision below for actual transfers.';byId('recommended-xi-heading').textContent=`Recommended GW${decision.event||1} XI`;byId('decision-status').className='status-good';byId('decision-status').textContent=`Active · ${selected.label} profile`;byId('decision-summary').innerHTML=`<div class="decision-metric"><b>£${Number(squad.cost).toFixed(1)}m</b><span>Squad cost</span></div><div class="decision-metric"><b>£${Number(squad.money_remaining).toFixed(1)}m</b><span>Money remaining</span></div><div class="decision-metric"><b>${Number(squad.projected_event_points_including_captain??squad.projected_gw1_points_including_captain).toFixed(1)}</b><span>Modeled GW${decision.event||1} points, captain included</span></div><div class="decision-metric"><b>${esc(squad.captain.name)}</b><span>Captain · ${Number(squad.captain.xp_1).toFixed(1)} xPts before doubling</span></div><div class="decision-metric"><b>${esc(squad.vice_captain.name)}</b><span>Vice-captain</span></div>`;
byId('profile-options').innerHTML=availableProfiles.map(profile=>{const profileMetrics=profile.metrics||{};return `<button id="profile-tab-${esc(profile.id)}" type="button" role="tab" aria-selected="${profile.id===selected.id}" aria-controls="profile-panel" tabindex="${profile.id===selected.id?'0':'-1'}" class="profile-option ${profile.id===selected.id?'active':''}" data-profile="${esc(profile.id)}"><strong>${esc(profile.label)}</strong><span>${esc(profile.summary)}</span><span>1 / 3 / 5 GW central · ${Number(profileMetrics.central_1gw||0).toFixed(1)} / ${Number(profileMetrics.central_3gw||0).toFixed(1)} / ${Number(profileMetrics.central_5gw||0).toFixed(1)} xPts</span><span>5-GW range ${Number(profileMetrics.lower_5gw||0).toFixed(1)}–${Number(profileMetrics.upper_5gw||0).toFixed(1)}</span></button>`;}).join('');byId('profile-panel').setAttribute('aria-labelledby',`profile-tab-${selected.id}`);bindTabs('profile-options','[data-profile]',button=>{renderDecision(button.dataset.profile);renderWeeklyDecision(button.dataset.profile);});
const comparison=selected.comparison_to_balanced||{};const changed=comparison.changed_players||{};const incoming=Array.isArray(changed)?changed:(changed.in||[]);const outgoing=Array.isArray(changed)?[]:(changed.out||[]);const changeText=selected.id==='balanced'?'Default central-projection reference team.':`${comparison.shared_players??0}/15 players shared with Balanced. ${incoming.length?`In: ${incoming.join(', ')}. `:''}${outgoing.length?`Out: ${outgoing.join(', ')}.`:''}`;byId('profile-comparison').innerHTML=`<div class="profile-stat"><b>${Number(metrics.central_1gw||0).toFixed(1)}</b><span>1-GW modeled xPts</span></div><div class="profile-stat"><b>${Number(metrics.central_3gw||0).toFixed(1)}</b><span>3-GW modeled xPts</span></div><div class="profile-stat"><b>${Number(metrics.central_5gw||0).toFixed(1)}</b><span>5-GW modeled xPts</span></div><div class="profile-stat"><b>${Number(metrics.average_expected_minutes||0).toFixed(0)}</b><span>Average expected minutes</span></div><div class="profile-stat"><b>${Number(metrics.average_ownership||0).toFixed(1)}%</b><span>Average ownership</span></div><div class="profile-stat"><b>${Number(metrics.low_confidence_players||0)}</b><span>Low-confidence players</span></div><div class="profile-explanation"><strong>${esc(selected.objective||selected.summary)}</strong><br><span class="muted">5-GW uncertainty range: ${Number(metrics.lower_5gw||0).toFixed(1)}–${Number(metrics.upper_5gw||0).toFixed(1)} xPts.</span><br>${esc(changeText)} <span class="status-wait">Main trade-off: ${esc(selected.risk_note)}</span></div>`;
byId('recommended-formation').textContent=`${esc(squad.formation)} · ${esc(selected.label)} · xPts shown for 1 / 3 / 5 GWs`;byId('recommended-xi').setAttribute('aria-label',`${squad.formation} recommended formation: ${(squad.starting_xi||[]).map(player=>`${player.name}${player.id===captainId?' captain':player.id===viceId?' vice-captain':''}`).join(', ')}`);byId('recommended-xi').innerHTML=pitch(squad.starting_xi||[]);byId('recommended-bench').innerHTML=(squad.bench||[]).map((player,index)=>card(player,index)).join('');byId('recommended-squad').innerHTML=(squad.players||[]).map(player=>card(player)).join('');byId('captaincy-list').innerHTML=(selected.captaincy||decision.captaincy||[]).map((player,index)=>`<div class="decision-row"><span><strong>${index+1}. ${esc(player.name)}</strong><br><span class="muted">${esc(player.club)} · ${Number(player.expected_minutes).toFixed(0)} expected min</span></span><b>${Number(player.xp_1).toFixed(1)} xPts</b></div>`).join('');const model=decision.model||{};const inputs=(model.inputs||[]).map(item=>`<div class="decision-note"><strong>Input</strong><br>${esc(item)}</div>`).join('');const limits=(model.limitations||[]).map(item=>`<div class="decision-note"><strong>Risk</strong><br>${esc(item)}</div>`).join('');byId('decision-model').innerHTML=`<div class="model-summary-grid"><div class="decision-note"><strong>${esc(model.name||'Projection model')} v${esc(model.version||'')}</strong><br>No betting odds. Generated ${esc(fmtDate(decision.generated_at,true))}.</div><div class="decision-note"><strong>${esc(selected.label)} objective</strong><br>${esc(selected.objective||selected.summary)}</div></div><details class="model-disclosure"><summary>Show model inputs and risks (${(model.inputs||[]).length} inputs · ${(model.limitations||[]).length} risks)</summary><div class="model-detail-grid">${inputs}${limits}</div></details>`;attachBreakdownHandlers(squad.players,squad.captain);}
function renderWeeklyDecision(profileId=null){
const weekly=decision.weekly_decisions||{};const ids=['weekly-profile-options','weekly-summary','weekly-recommendation','weekly-scenarios','weekly-chip','weekly-rules'];if(weekly.status!=='active'){byId('weekly-plan').hidden=true;byId('weekly-branches').innerHTML='';byId('weekly-lineup').hidden=true;byId('weekly-status').className='status-wait';byId('weekly-status').textContent=weekly.status==='waiting_for_gw2'?'Starts in Gameweek 2':'Published squad required';byId('weekly-profile-options').innerHTML='';byId('weekly-summary').innerHTML='';byId('weekly-recommendation').innerHTML=`<div class="empty">${esc(weekly.reason||'Weekly recommendations activate after the first published squad is available.')}</div>`;byId('weekly-scenarios').innerHTML='';byId('weekly-chip').innerHTML='';byId('weekly-rules').innerHTML='';return;}
const profiles=weekly.profiles||[];const selectedId=profileId||weekly.default_profile||'balanced';const selected=profiles.find(row=>row.id===selectedId)||profiles[0];if(!selected)return;const recommendation=selected.recommendation;const plan=selected.multiweek_plan||{};const actionLabels={roll:'Roll the transfer',single_transfer:'Make one transfer',double_transfer:'Make two transfers'};const actionLabel=actionLabels[recommendation.action]||recommendation.action;const transferPairs=(recommendation.transfers||[]).map(move=>`<div class="transfer-pair"><strong>${esc(move.out.name)} → ${esc(move.in.name)}</strong><span class="muted">Sell £${Number(move.out.selling_price).toFixed(1)}m · buy £${Number(move.in.price).toFixed(1)}m</span></div>`).join('')||'<div class="transfer-pair"><strong>No transfer</strong><span class="muted">Preserve the free transfer for the next deadline.</span></div>';
byId('weekly-heading').textContent=`Gameweek ${weekly.event} roll, transfer, and chip decision`;byId('weekly-status').className='status-good';byId('weekly-status').textContent=`Active · ${selected.label}`;byId('weekly-profile-options').innerHTML=profiles.map(profile=>{const profilePlan=profile.multiweek_plan||{};return `<button id="weekly-profile-tab-${esc(profile.id)}" type="button" role="tab" aria-selected="${profile.id===selected.id}" aria-controls="weekly-profile-panel" tabindex="${profile.id===selected.id?'0':'-1'}" class="profile-option ${profile.id===selected.id?'active':''}" data-weekly-profile="${esc(profile.id)}"><strong>${esc(profile.label)}</strong><span>${esc(profile.summary)}</span><span>${esc(actionLabels[profile.recommendation.action]||profile.recommendation.action)} · ${Number(profilePlan.five_gameweek_advantage_over_roll||0).toFixed(1)} planner edge vs roll</span></button>`;}).join('');byId('weekly-profile-panel').setAttribute('aria-labelledby',`weekly-profile-tab-${selected.id}`);bindTabs('weekly-profile-options','[data-weekly-profile]',button=>{renderWeeklyDecision(button.dataset.weeklyProfile);renderDecision(button.dataset.weeklyProfile);});
byId('weekly-summary').innerHTML=`<div class="decision-metric"><b>${weekly.free_transfers}</b><span>Free transfer${weekly.free_transfers===1?'':'s'} now · ${weekly.free_transfer_source==='confirmed_local'?'confirmed locally':'estimated from public history'}</span></div><div class="decision-metric"><b>${recommendation.free_transfers_next_event}</b><span>Available next GW</span></div><div class="decision-metric"><b>${Number(plan.five_gameweek_advantage_over_roll||0).toFixed(1)}</b><span>5-GW planner edge over roll</span></div><div class="decision-metric"><b>${recommendation.point_cost?`−${recommendation.point_cost}`:'0'}</b><span>Immediate transfer cost</span></div><div class="decision-metric"><b>£${Number(recommendation.bank_after).toFixed(1)}m</b><span>Bank after decision</span></div>`;
byId('weekly-recommendation').innerHTML=`<strong>${esc(actionLabel)}</strong><p>${esc(recommendation.reason)}</p>${transferPairs}<span class="muted">Captain ${esc(recommendation.captain.name)} · vice-captain ${esc(recommendation.vice_captain.name)} · ${esc(recommendation.formation)} · ${Number(recommendation.projected_event_points_including_captain).toFixed(1)} modeled GW${weekly.event} points</span>`;
byId('weekly-scenarios').innerHTML=(selected.scenarios||[]).map(scenario=>{const planned=(plan.alternatives||[]).find(item=>item.action===scenario.action)||{};return `<div class="scenario-card ${scenario.action===recommendation.action?'recommended':''}"><strong>${esc(actionLabels[scenario.action]||scenario.action)}</strong><span>${scenario.transfer_count} transfer${scenario.transfer_count===1?'':'s'} · ${scenario.point_cost?`−${scenario.point_cost} hit`:'no hit'}</span><span class="projection">${Number(planned.five_gameweek_delta_vs_roll||0).toFixed(1)} planner edge vs roll</span><span>${Number(scenario.net_gain_5gw).toFixed(1)} direct 5-GW net · £${Number(scenario.bank_after).toFixed(1)}m bank · ${scenario.free_transfers_next_event} FT next GW</span></div>`;}).join('');
const branches=plan.conditional_branches||[];byId('weekly-plan').hidden=!plan.planning_method;if(plan.planning_method){byId('weekly-plan-confidence').textContent=`${esc(plan.confidence||'low')} confidence`;byId('weekly-plan-summary').innerHTML=`<div class="weekly-plan-stat"><b>${Number(plan.five_gameweek_advantage_over_roll||0).toFixed(1)}</b><span>5-GW advantage over roll</span></div><div class="weekly-plan-stat"><b>${Number(plan.roll_option_value||0).toFixed(1)}</b><span>Modeled value of the extra rolled transfer</span></div><div class="weekly-plan-stat"><b>${esc((plan.horizon_events||[]).map(event=>`GW${event}`).join('–'))}</b><span>Receding planning horizon</span></div>`;byId('weekly-branches').innerHTML=branches.map(branch=>{const branchLabel=actionLabels[branch.action]||branch.action;return `<div class="conditional-branch"><strong>GW${branch.event}: ${esc(branchLabel)} · provisional</strong><span>${esc(branch.condition)}</span><span>${branch.point_cost?`Potential −${branch.point_cost} hit · `:''}${branch.free_transfers_before} FT before · ${branch.free_transfers_next_event} FT next</span></div>`;}).join('')||'<div class="empty">No future action clears the current hold path. Recalculate after the next explicit refresh.</div>';byId('weekly-plan-assumptions').innerHTML=(plan.assumptions||[]).map(item=>`<div class="decision-note">${esc(item)}</div>`).join('');}
const weeklyCaptainId=recommendation.captain&&recommendation.captain.id;const weeklyViceId=recommendation.vice_captain&&recommendation.vice_captain.id;const weeklyPitch=(lineup)=>['FWD','MID','DEF','GKP'].map(position=>{const row=(lineup||[]).filter(player=>player.position_short===position);return `<div class="pitch-row pitch-${position.toLowerCase()}">${row.map(player=>{const role=player.id===weeklyCaptainId?'C':player.id===weeklyViceId?'VC':'';return `<div class="pitch-player ${player.id===weeklyCaptainId?'captain':''}" title="${esc(player.name)} · ${esc(player.club)}"><strong>${esc(player.name)}${role?` (${role})`:''}</strong><span>${esc(player.club)}</span><span class="projection">${Number(player.xp_1).toFixed(1)} / ${Number(player.xp_3).toFixed(1)} / ${Number(player.xp_5).toFixed(1)}</span></div>`;}).join('')}</div>`;}).join('');byId('weekly-lineup').hidden=false;byId('weekly-lineup-heading').textContent=`Recommended GW${weekly.event} XI · ${actionLabel}`;byId('weekly-lineup-meta').textContent=`${recommendation.formation} · ${selected.label}`;byId('weekly-pitch').setAttribute('aria-label',`${recommendation.formation} post-decision formation: ${(recommendation.starting_xi||[]).map(player=>`${player.name}${player.id===weeklyCaptainId?' captain':player.id===weeklyViceId?' vice-captain':''}`).join(', ')}`);byId('weekly-pitch').innerHTML=weeklyPitch(recommendation.starting_xi||[]);byId('weekly-bench').innerHTML=(recommendation.bench||[]).map((player,index)=>`<div class="weekly-bench-card"><strong>${index+1}. ${esc(player.name)}</strong><span>${esc(player.position_short)} · ${esc(player.club)}</span><span>${Number(player.xp_1).toFixed(1)} / ${Number(player.xp_3).toFixed(1)} / ${Number(player.xp_5).toFixed(1)} xPts</span></div>`).join('');
const chip=selected.chip_recommendation||{};const alternatives=(chip.alternatives||[]).map(item=>`<div class="chip-alternative"><strong>${esc(item.label)}</strong><br>${Number(item.marginal_value).toFixed(1)} marginal xPts<br><span class="muted">Threshold ${Number(item.threshold).toFixed(1)}</span></div>`).join('');byId('weekly-chip').innerHTML=`<strong>${esc(chip.label||'Hold all chips')}</strong><p>${esc(chip.reason||'No chip recommendation is available.')}</p><span class="muted">No-chip baseline: ${Number(chip.no_chip_projected_points||0).toFixed(1)} projected GW${weekly.event} points</span>${alternatives?`<div class="chip-alternatives">${alternatives}</div>`:''}`;
const inventory=(weekly.chip_inventory||[]).map(item=>`${item.label}: ${item.available?'available':item.used_event?`used GW${item.used_event}`:`outside GW${item.start_event}–${item.stop_event}`}`).join(' · ');const rules=weekly.official_rules||{};byId('weekly-rules').innerHTML=`<strong>Official rules reviewed</strong><br>One free transfer per Gameweek, up to ${esc(rules.maximum_free_transfers)} stored; each excess transfer costs ${esc(rules.extra_transfer_cost)} points. ${esc(inventory)}<br>${safeLink(rules.source,'Official FPL rules')}<br><span class="status-wait">${esc(weekly.state_warning)}</span>`;}
function renderManager(){const manager=state.manager||{connection_status:'not_configured',squad:[]};const value=value=>value===null||value===''||value===false?'Not yet available':value;const money=value=>value===null||value===''?'Not yet available':`£${(Number(value)/10).toFixed(1)}m`;if(manager.connection_status==='not_configured'){byId('my-team-summary').innerHTML='<div class="empty">No public team ID is configured.</div>';byId('manager-status').textContent='Add a public team ID to connect My Team.';byId('squad-grid').innerHTML='<div class="empty">No public squad is connected.</div>';return;}byId('my-team-summary').innerHTML=`<div class="team-stat"><b>${esc(manager.team_name||'Unnamed team')}</b><span>${esc(manager.manager_name||'Manager')}</span></div><div class="team-stat"><b>${esc(manager.team_id)}</b><span>Team ID</span></div><div class="team-stat"><b>${esc(value(manager.overall_rank))}</b><span>Overall rank</span></div><div class="team-stat"><b>${esc(value(manager.overall_points))}</b><span>Points</span></div><div class="team-stat"><b>${esc(money(manager.team_value))}</b><span>Team value</span></div><div class="team-stat"><b>${esc(money(manager.bank))}</b><span>Bank</span></div>`;if(!manager.squad_publicly_available){byId('manager-status').className='empty';byId('manager-status').textContent='Connected to the official public entry. Public GW1 squad is hidden until the deadline, so no draft players are inferred.';byId('squad-grid').innerHTML='<div class="empty">Public GW1 squad is hidden until the deadline. The dashboard will load it after official FPL publishes the picks.</div>';return;}byId('manager-status').className='status-good';byId('manager-status').textContent=`Connected · ${manager.squad.length} public picks loaded`;const positions={1:'Goalkeeper',2:'Defender',3:'Midfielder',4:'Forward'};byId('squad-grid').innerHTML=manager.squad.map(player=>`<div class="player-card"><strong>${esc(player.name)}${player.is_captain?' (C)':player.is_vice_captain?' (VC)':''}</strong><span>${esc(positions[player.element_type]||'Player')} · ${esc(money(player.price))}</span><span>${player.position<=11?'Starting XI':`Bench ${player.position-11}`}</span></div>`).join('');}
function renderPerformance(){const summary=performance.summary||{};const calibration=performance.calibration||{recommendations:[]};const format=value=>value==null?'Awaiting':Number(value).toFixed(1);const coverage=summary.range_coverage==null?'Awaiting':`${(Number(summary.range_coverage)*100).toFixed(0)}%`;const active=performance.status==='active';byId('performance-status').className=active?'status-good':'status-wait';byId('performance-status').textContent=active?`${performance.completed_comparisons} completed comparison${performance.completed_comparisons===1?'':'s'}`:'Starts after Gameweek 1 is final';byId('performance-summary').innerHTML=`<div class="decision-metric"><b>${Number(performance.completed_comparisons||0)}</b><span>Completed profile-horizon comparisons</span></div><div class="decision-metric"><b>${Number(performance.actual_events_collected||0)}</b><span>Official Gameweeks collected</span></div><div class="decision-metric"><b>${format(summary.mae)}</b><span>Mean absolute error</span></div><div class="decision-metric"><b>${format(summary.bias)}</b><span>Bias · actual minus modeled</span></div><div class="decision-metric"><b>${coverage}</b><span>Uncertainty-range coverage</span></div>`;const horizons=performance.by_horizon||{};byId('performance-horizons').innerHTML=[1,3,5].map(horizon=>{const row=horizons[String(horizon)]||{};return `<div class="performance-card"><b>${format(row.mae)}</b><span>${horizon}-GW MAE · ${Number(row.count||0)} completed</span><span>Bias ${format(row.bias)} · coverage ${row.range_coverage==null?'Awaiting':`${(Number(row.range_coverage)*100).toFixed(0)}%`}</span></div>`;}).join('');byId('performance-calibration').innerHTML=`<p class="${calibration.ready?'status-good':'status-wait'}">${esc(calibration.status||'Waiting for enough completed forecasts.')}</p>${(calibration.recommendations||[]).map(item=>`<div class="decision-note">${esc(item)}</div>`).join('')}<div class="decision-note"><strong>Improvement policy</strong><br>Use error, bias, and interval coverage to review minutes, scoring-rate, and uncertainty assumptions. No weights are silently retuned from a small sample.</div>`;const comparisons=performance.comparisons||[];byId('performance-history').innerHTML=comparisons.length?comparisons.slice(0,50).map(row=>{const error=Number(row.error);return `<tr class="performance-row"><th scope="row">GW${row.origin_event}</th><td>${esc(row.profile_label)}</td><td>${row.horizon} GW</td><td>${Number(row.modeled_points).toFixed(1)}</td><td>${Number(row.actual_points).toFixed(1)}</td><td class="${error>0?'positive':error<0?'negative':''}">${error>0?'+':''}${error.toFixed(1)}</td><td>${row.inside_range?'Yes':'No'}</td></tr>`;}).join(''):'<tr><td colspan="7"><div class="empty">No completed comparison yet. The current pre-GW1 forecast is stored locally and will be scored after official results are final.</div></td></tr>';byId('performance-method').textContent=performance.method||'Frozen pre-event forecasts are compared with official FPL points.';const collectionErrors=performance.collection_errors||[];byId('performance-errors').innerHTML=collectionErrors.map(error=>`<div class="limitation-note"><strong>Result collection issue</strong>${esc(error.message||error)}</div>`).join('');}
function renderModel(){byId('model-readiness').innerHTML=`<dt>User-facing state</dt><dd>${esc(seasonLabel())}</dd><dt>Raw status</dt><dd>${esc(state.fpl.season_status)}</dd><dt>Phase</dt><dd>${esc(state.fpl.season_phase||'feed_pending')}</dd><dt>Players</dt><dd>${state.fpl.player_count}</dd><dt>Clubs</dt><dd>${state.fpl.team_count}</dd><dt>Decision model</dt><dd class="${decision.status==='active_preliminary'?'status-good':'status-wait'}">${esc(decision.status)}</dd><dt>Projection version</dt><dd>${esc((decision.model||{}).version||'Not active')}</dd><dt>Generated</dt><dd>${esc(state.generated_at)}</dd>`;const sources=state.sources||[];byId('source-list').innerHTML=sources.length?sources.map(source=>`<div class="source">${safeLink(source.url,source.name)}<span>Configured</span></div>`).join(''):'<div class="empty">No sources are registered.</div>';}
function captureWorkspaceContext(){const activeView=(document.querySelector('.view.active')||{}).id?.replace('view-','')||'overview';const activeProfile=(document.querySelector('[data-profile][aria-selected="true"]')||{}).dataset?.profile||null;const controlIds=[...filterIds,'player-search','player-club-filter','player-position-filter','player-sort','fixture-gameweek','fixture-club-filter'];const controls=Object.fromEntries(controlIds.map(id=>[id,byId(id)?.value]).filter(([,value])=>value!==void 0));sessionStorage.setItem('fpl-workspace-context',JSON.stringify({view:activeView,profile:activeProfile,controls}));}
function restoreWorkspaceContext(){let context={};try{context=JSON.parse(sessionStorage.getItem('fpl-workspace-context')||'{}');}catch(error){sessionStorage.removeItem('fpl-workspace-context');}Object.entries(context.controls||{}).forEach(([id,value])=>{const control=byId(id);if(control&&[...control.options||[]].some(option=>option.value===value)||control?.type==='search')control.value=value;});if(context.profile){renderDecision(context.profile);renderWeeklyDecision(context.profile);}renderPlayers();renderFixtures();applyFilters();showView(titles[context.view]?context.view:'overview');}
function sourceSummary(payload){const sources=payload.source_statuses||{};const mark=value=>value==='ok'?'✓':value==='error'?'✕':'not active';return `FPL ${mark(sources.fpl)} · My Team ${mark(sources.manager)} · Transfers ${mark(sources.transfers)} · Fixtures ${mark(sources.fixtures)}`;}
function setupRefresh(){const button=byId('refresh-now');const message=byId('refresh-message');const sourceStatus=byId('refresh-source-status');const token=document.querySelector('meta[name="refresh-token"]').content;const available=location.protocol.startsWith('http')&&token&&!token.includes('__REFRESH_TOKEN__');const saved=sessionStorage.getItem('fpl-refresh-result');if(saved){try{const result=JSON.parse(saved);message.textContent=`Updated ${fmtDate(result.generated_at,true)}`;sourceStatus.textContent=sourceSummary(result);}catch(error){sessionStorage.removeItem('fpl-refresh-result');}}if(!available){button.disabled=true;if(!saved)message.textContent='Start the local dashboard service to enable refresh.';return;}if(!saved)message.textContent=`Last refreshed ${fmtDate(state.generated_at,true)} · refreshes only when you choose.`;button.addEventListener('click',async()=>{button.disabled=true;button.textContent='Refreshing…';message.textContent='Checking official FPL and Premier League sources.';sourceStatus.textContent='';try{const response=await fetch('/api/refresh',{method:'POST',headers:{'X-Refresh-Token':token,'Content-Type':'application/json'},body:'{}'});const payload=await response.json().catch(()=>({message:'Refresh returned an unreadable response.'}));if(!response.ok)throw new Error(payload.message||`Refresh failed with status ${response.status}`);captureWorkspaceContext();sessionStorage.setItem('fpl-refresh-result',JSON.stringify(payload));message.textContent='Refresh complete. Reloading…';window.setTimeout(()=>window.location.reload(),500);}catch(error){button.disabled=false;button.textContent='Refresh now';message.textContent=`Refresh failed: ${error.message}`;sourceStatus.textContent='Previous snapshots remain in use.';}});}
setupRefresh();populateFilters();setupPlayerExplorer();setupFixtures();renderOverview();renderDecision();renderWeeklyDecision();renderManager();renderPerformance();renderModel();restoreWorkspaceContext();
</script></body></html>'''


def render_dashboard(state):
    data = json.dumps(state, ensure_ascii=False).replace("</", "<\\/")
    trusted_domains = json.dumps(_TRUSTED_LINK_DOMAINS, ensure_ascii=True)
    return (
        _TEMPLATE.replace("__DASHBOARD_DATA__", data)
        .replace("__TRUSTED_LINK_DOMAINS__", trusted_domains)
    )
