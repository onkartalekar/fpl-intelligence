# 2026-08-11 -- 19 changes shipped

19 changes shipped today.

- **[Feature]** Render dashboard fresh for no-team_id visitors instead of a stale static file (issue #120) -- Closes #120
- **[Feature]** Trigger a scheduled refresh at T-2d/T-1d/T-12h/T-3h before each gameweek deadline (issue #101) -- Closes #101
- **[Data]** Read real official transfer data into deadline-reminder recommendations (issue #122) -- Closes #122
- **[Data]** Single source of truth: GitHub-Actions-hosted scripts read Railway's live state over HTTP (issue #125) -- Closes #125
- **[Feature]** Reminder job reads opted-in team list over HTTP, not Railway's local disk (issue #105) -- Closes the last instance of the "GitHub-Actions-hosted script assumes local filesystem access to Railway's `profiles.db`" bug class (same root cause as #101/#122/#125). See [plans/issue-105-reminder-teams-endpoint.md](plans/issue-105-reminder-teams-endpoint.md) for the full candidate analysis (Railway-cron-with-shared-volume vs. a new read endpoint) and the decided token question.
- **[Feature]** Remove the manager profile goal field entirely (issue #117) -- While implementing #117's original ask (make `beat_last_season` real), re-examined whether *any*
- **[Fix]** Live regression-test agent for the hosted Railway app (issue #119) -- Nothing today exercises the real deployed Railway origin end-to-end -- `tests/*.py` runs
- **[Feature]** Document system and model architecture visually with Mermaid diagrams (issue #127) -- Zero diagrams existed across README.md/SPECIFICATION.md/IMPLEMENTATION_PLAN.md/MODEL.md. See
- **[Fix]** Drop the stale goal field from the live regression check's profile check -- (PR #131) still sent it in `check_profile_endpoint`'s payload -- I'd flagged this exact scenario
- **[Feature]** Simplify the topbar subtitle: drop stale objective text and repetitive tagline -- You flagged the topbar subtitle ("Top-50k objective · balanced risk · Eastern Time (New York) ·
- **[Fix]** Fix pitch-diagram player card truncation on mobile (issue #133) -- See [plans/issue-133-pitch-mobile-truncation.md](plans/issue-133-pitch-mobile-truncation.md) for
- **[Feature]** Generalize per-team forecast archiving beyond the one legacy team (issue #102) -- See [plans/issue-102-per-team-forecast-archiving.md](plans/issue-102-per-team-forecast-archiving.md)
- **[Docs]** Bring README's Environment variables section up to date -- The section hadn't tracked #101/#102/#105/#119/#125 and had gone actively wrong in two places:
- **[Feature]** Don't fail the reminder workflow when zero teams have opted in yet -- Real production failure from the first live run: `No reminder recipients configured` -- because
- **[Feature]** Unify SMTP credentials to a single FPL_INTEL_SMTP_* name -- Renames the live server's SMTP env vars (`FPL_INTEL_SERVER_SMTP_*`, read by `reminder_confirmation.py` for the reminder-confirmation and Contact Us sends) and `live_regression_check.py`'s IMAP-read env vars to a single canonical `FPL_INTEL_SMTP_*`, matching what `send_deadline_reminder.py` already used.
- **[Feature]** Don't require SMTP config on cron ticks that never send an email -- `send_deadline_reminder.py`'s `main()` required `FPL_INTEL_SMTP_*` env vars on every real (non `--dry-run`) invocation, even when zero teams were in this hour's reminder window -- so the hourly scheduled cron would fail every single tick until SMTP was configured, regardless of whether an email would ever actually be sent.
- **[Fix]** Search the Contact Us notification's body, not its Subject, for the live-regression-check marker -- `_imap_poll_for_marker` (issue #119's live-regression-check script) searched the email **Subject** header for `[live-regression-check]`/the run ID. `compose_contact_email` (`reminder_confirmation.py`) always sets the Subject to `"FPL Intelligence contact form: <category>"`, regardless of what's submitted -- the marker and run ID are only ever in the message **body** (the submitted "Message:" field). The old SUBJECT search could never have matched, no matter how long it polled.
- **[Docs]** Plan doc: daily automated What's New tab (issue #143) -- Research/design plan for issue #143 -- no code changes, doc only.
- **[Docs]** Plan doc: What's New tab UX design (issue #143) -- Updates the issue #143 plan doc with the reviewed UX mockup -- doc only, no code changes.
