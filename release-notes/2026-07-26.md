# 2026-07-26 -- 8 changes shipped

8 changes shipped today.

- **[Feature]** Surface an in-UI reminder to configure the FPL profile -- Overview's "What needs attention" panel now shows a Setup item when no FPL team ID is configured, pointing to the exact file/field to edit and linking straight to My Team.
- **[Fix]** Redesign Decision Center for scannability, fix subnav/mobile scroll bugs -- Sticky player detail side panel with click-to-highlight, replacing the bottom-of-page block
- **[Data]** Clarify that data/history is only for model refitting, not live use -- `README.md` and `MODEL.md` both mention `data/history/` (four seasons of prior-year data, committed in full) without explaining why it's there or that it's unused by the live dashboard
- **[Feature]** Add a coefficient provenance table to MODEL.md -- `MODEL.md` explained the fit/validate workflow but never indexed which of the ~15 keys in `config/model-coefficients.json` are actually fitted (empirically or by search) versus hand-set, or where each is consumed in code
- **[Feature]** Explain what each model coefficient means and why it matters -- The existing provenance table said how each coefficient in `config/model-coefficients.json` is set and where it's consumed, but not what its value represents or why it's significant to a projection
- **[Docs]** Expand FPL jargon and acronyms on first use in the docs -- `README.md`/`MODEL.md` used `FPL`, `GKP`/`DEF`/`MID`/`FWD`, `XI`, `xG`/`xA`, and `MAE` throughout without ever spelling them out
- **[Feature]** Investigate ICT Index as a projection signal -- clean negative result -- FPL's official `ict_index` field is already present in `data/history/` but unused by the model
- **[Data]** Record npxG/xT/SCA/GCA data-availability research, decline for now -- Documents research into whether non-penalty xG, expected threat, and shot-/goal-creating actions could strengthen the attacking projection
