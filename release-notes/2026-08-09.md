# 2026-08-09 -- 17 changes shipped

17 changes shipped today.

- **[Feature]** Email a transfer-deadline reminder with recommendations (issue #55) -- Closes #55
- **[Feature]** Shadow-mode ML minutes challenger (issue #65) -- Closes #65
- **[Docs]** docs: record complete dashboard feature demo -- refresh the tracked demo dashboard with the current UI and Model Performance state
- **[Feature]** Plan issue #81: apply confirmed-free-transfer/draft-squad overrides in the reminder -- Planning-only pass for #81. Investigated the issue's claim that this is a small, self-contained wire-through with no open design questions -- confirmed it against the real code:
- **[Feature]** Plan issue #82: include all three risk profiles in the reminder email -- Plan-only pass for #82 (no code changes). Investigated whether extending `scripts/send_deadline_reminder.py`'s email composer to show all three risk profiles (conservative/balanced/aggressive), instead of just the default one, is genuinely uncontested for both weekly-decisions states the script handles.
- **[Feature]** Plan issue #80: reminder recipients from profiles.db -- Plan-only pass for #80 (deadline reminder reading opted-in recipients from `profiles.db` instead of only the static `FPL_INTEL_REMINDER_TEAMS` secret). No code changes -- adds `plans/issue-80-reminder-source-profiles-db.md`.
- **[Feature]** Plan issue #78: manager goal field -- Planning-only pass for #78 (add a manager `goal` field to the profile, default top-50k). No code changes -- adds `plans/issue-78-manager-goal.md` only.
- **[Feature]** Plan issue #83: HTML/multimedia reminder email template -- Planning-only pass for #83 (deadline-reminder HTML/multimedia email template) -- no code changes, per the repo's `/plan-issue` process. Adds `plans/issue-83-reminder-html-email.md`.
- **[Feature]** Plan issue #79: self-serve deadline-reminder opt-in -- Planning-only pass for #79 (no code changes). The issue's UX placement (extend the existing Profile tab + tri-state attention-banner nudge) was already settled in the issue body and isn't re-litigated. This plan resolves the genuinely open question: the abuse-mitigation design for a public write path that causes a real inbox to start receiving recurring email.
- **[Feature]** Wire saved profile overrides into the deadline reminder (issue #81) -- `scripts/send_deadline_reminder.py`'s `run()` called `compute_manager_view(bootstrap, fixtures, [], generated_at, team["team_id"])` without `confirmed_free_transfers`, `confirmed_free_transfers_event`, or `draft_squad_ids` — the one caller of `compute_manager_view` in the codebase that silently ignored a team's saved profile overrides. `server.py`'s `_default_team_view_action` already loaded and passed all three from `profiles.load_profile()`.
- **[Feature]** Deadline reminder: include all three risk profiles in the email body -- `compose_email()`'s two section-composer helpers in `scripts/send_deadline_reminder.py` only ever surfaced one risk profile, even though the model already computes all three (conservative/balanced/aggressive) before `compose_email()` runs:
- **[Feature]** Add a manager goal field to the profile (issue #78) -- Closes #78
- **[Feature]** Self-serve deadline-reminder opt-in: confirmation-link double opt-in (issue #79) -- Closes #79
- **[Feature]** Deadline reminder: read opted-in recipients from profiles.db (issue #80) -- Closes #80
- **[Feature]** Update issue #83 plan with reviewed mockup (table + inline-SVG together) -- Updates `plans/issue-83-reminder-html-email.md` and adds the reviewed mockup at `plans/assets/issue-83-reminder-email-mockup.pdf` for issue #83.
- **[Fix]** Fix profiles.db schema migration: existing databases silently miss new columns -- A live dashboard server (this repo's own dev instance) hit:
- **[Feature]** Plan issue #27: hosting strategy — public + Railway, token-gate split, observability -- Updates `plans/issue-27-cloud-hosting.md` with the hosting strategy resolved via an interview with the user, plus one structural finding that fell out of grounding the refresh-trigger question in the actual code. **Plan doc only — no application code changed.**
