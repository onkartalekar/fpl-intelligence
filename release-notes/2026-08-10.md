# 2026-08-10 -- 11 changes shipped

11 changes shipped today.

- **[Feature]** Deadline reminder: HTML/multimedia email template (issue #83) -- Adds an HTML alternative to the deadline reminder email alongside the existing plain-text body, per the reviewed mockup (`plans/assets/issue-83-reminder-email-mockup.pdf`) and `plans/issue-83-reminder-html-email.md`'s "Mockup review" section — building the table-based layout and the inline-SVG starting-XI pitch diagram together in one pass, as that section directs.
- **[Docs]** docs: add environment-variable inventory to README (issue #27 prep) -- Adds a new `## Environment variables` section to `README.md` documenting every `FPL_INTEL_*`/`ANTHROPIC_API_KEY` env var actually read by the codebase today, grouped by feature:
- **[Feature]** Host the dashboard on the cloud (issue #27): env-driven host/port/token/origin -- Implements the "Concrete code changes (implementation-ready sketch)" section of
- **[Chore]** skills: add file-issue and merge-pr, update run-full-tests and ship-issue -- Two new skills and two updates, all based on patterns repeated many times over this session with no skill backing them.
- **[Feature]** Close remaining issue #28 gaps: /api/refresh cooldown + connection timeout -- Closes #28
- **[Fix]** Fix Railway volume shadowing tracked seed files under data/ (POST /api/refresh crash) -- Fixes #107
- **[Fix]** Fix reminder lead-time picker: replace unstyled radio inputs with the existing profile-option tab pattern -- The deadline-reminder lead-time selector (My Profile → Deadline reminders) used three raw `<input type="radio">` elements with zero custom styling — checked `dashboard.css` directly, there's no `input[type=radio]` rule anywhere in it. It relied entirely on native browser/device rendering, which on the reporting device produced oversized circles and the label text wrapping mid-word ("T-" / "3H" on separate lines). It was also visually inconsistent with the rest of the app: risk-profile…
- **[Feature]** Gate Decision Center and Model Performance behind a profile empty-state (issue #108) -- Closes #108
- **[Feature]** Publish alpha RELEASE_NOTES.md (issue #112) -- Adds `RELEASE_NOTES.md` at the repo root: a plain-English, code-verified feature inventory for alpha testers who know FPL but haven't seen this tool. Scoped strictly to what's live on `main` today (no assumption that open issues #101/#102/#105/#108/#110 are done).
- **[Docs]** skills: document a doc-only test-skip exception in ship-issue -- Codifies, in the repo's own tooling, what was previously only a one-off in-conversation instruction: skipping the full test suite for a Markdown-only change (as happened on #113).
- **[Feature]** Add a Contact Us tab (issue #110): email + local-log durability backstop -- Implements issue #110 per the plan doc's decided direction (`plans/issue-110-contact-us-tab.md`): **Candidate 1, email-only, reusing issue #79's SMTP pattern, plus a local-log durability backstop** -- no persisted store, no admin-auth review surface, no GitHub connection.
