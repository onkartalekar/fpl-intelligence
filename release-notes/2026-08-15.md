# 2026-08-15 -- Streamlined UI and enhanced performance lead today's updates!

We've made significant improvements to our fantasy football dashboard, enhancing usability by removing redundant input fields and simplifying user interactions. On the performance side, we've optimized key functions for faster processing and integrated crucial logging features for better tracking of user actions and system performance.

- **[Fix]** Remove redundant transfer input fields -- Cleared out the unused 'Confirmed free transfers' and 'Free transfers gameweek' inputs to streamline the user profile form.
- **[Feature]** Add HEAD request support -- Implemented HEAD request functionality to support health checks, improving server logging.
- **[Fix]** Eliminate duplicate risk profile selector -- Removed redundant risk profile selectors from the 'Personalized weekly decision' section for clarity.
- **[Feature]** Log structured JSON access lines -- Updated logging format to structured JSON to enhance API call tracking.
- **[Fix]** Catch socket.timeout in error logging -- Enhanced error logging by catching both TimeoutError and socket.timeout exceptions.
- **[Chore]** Add CI workflow for test suite -- Introduced a continuous integration workflow to automatically run tests on every pull request.
- **[Chore]** Split tests workflow into parallel jobs -- Optimized the test suite by splitting it into parallel tasks to reduce overall runtime.
- **[Feature]** Memoize player scores in transfer calculations -- Improved the efficiency of transfer decision calculations through memoization.
- **[Feature]** Add local parallel test runner -- Implemented a local parallel test runner to mirror the CI test matrix, speeding up local testing.
- **[Feature]** Add benchmark for transfer build times -- Introduced a benchmark for the build_transfer_decisions function to measure performance against real data.
- **[Feature]** Memoize profile event scores -- Optimized the profile event scoring function to improve performance during transfer decision calculations.
- **[Docs]** Review documentation for accuracy -- Updated several documentation files to fix stale references and inaccuracies.
- **[Chore]** Untrack generated performance data -- Removed tracked generated artifacts to prevent unnecessary pollution of the repository.
- **[Feature]** Support for multiple free transfers -- Enhanced transfer search functionality to allow up to the official maximum free transfers.
- **[Fix]** Recalibrate chip thresholds -- Adjusted chip threshold values to align with the new scoring system after recent changes.
- **[Fix]** Update benchmark reference styling -- Muted the reference label in the benchmark panel to make it clear that it is not a recommended action.
