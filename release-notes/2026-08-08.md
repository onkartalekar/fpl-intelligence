# 2026-08-08 -- 26 changes shipped

26 changes shipped today.

- **[Fix]** Fix transfer-to-FPL-player name matching gaps -- Fixes two distinct root causes in `relevance.py::enrich_transfers()` that were causing real, current top-flight players to go unmatched against the FPL bootstrap feed, which silently dropped their `fpl_relevance` to `low` (filtering them out of the Transfers & News tab's default view) and made them ineligible for #31's role-transition/teammate-impact minutes adjustments.
- **[Fix]** Fix club attribution for cross-reported transfers -- A move confirmed by both the selling and buying club's own transfer-centre feeds gets merged into a single record by `refresh.py`'s cross-source dedup, which keeps only one side's `premier_league_club`/`movement_direction` attribution (whichever raw record happened to be processed last). Two consumers of that single field were both wrong as a result:
- **[Feature]** Add reusable Claude Code skills for issue-driven dev workflow -- Adds four Claude Code skills under `.claude/skills/`, carved out of the otherwise gitignored `.claude/` directory (`.claude/worktrees/` and other local state remain ignored):
- **[Feature]** Fixtures tab: replace Gameweek dropdown with Prev/Next navigation -- Replaces the Fixtures tab's Gameweek `<select>` dropdown with Prev/Next navigation buttons + a "Gameweek N" label, so stepping through gameweeks is a single click instead of opening a dropdown.
- **[Data]** Refresh official transfers data (2026-08-08) -- Data-only refresh of `data/official-transfers-latest.json` via `scripts/refresh_dashboard.py`, picking up new confirmed transfer activity since the last commit — including Wes Burns' departure from Ipswich and Cristian Orozco's arrival at Man Utd. 259 confirmed movements total.
- **[Feature]** Add plan-issue skill for open-ended research/design questions -- Adds a fifth Claude Code skill, `plan-issue`, modeled directly on the issue #31 workflow (transfer-driven team strength / teammate minutes-impact investigation) — for issues that are genuine research/design questions with more than one viable direction, as opposed to bugs with one obvious fix.
- **[Feature]** Cloud hosting investigation: plan for per-user teams (#27) -- Adds `plans/issue-27-cloud-hosting.md`, the investigation plan for #27 (host the dashboard on the cloud).
- **[Feature]** Theme/redesign investigation: decline palette overhaul, defer general redesign (#48) -- Records the confirmed decisions from investigating issue #48's three bundled requests (full investigation in `plans/issue-48-theme-redesign.md`):
- **[Feature]** Add light/dark theme toggle via full color tokenization -- Implements candidate (a) from `plans/issue-48-theme-redesign.md`: a light/dark theme toggle, built as a full color-tokenization pass rather than a quick variable swap.
- **[Feature]** Regenerate dashboard.html automatically after git merge/checkout -- `dashboard.html` is correctly gitignored -- a ~1MB fully-reproducible build artifact -- but nothing kept it in sync locally with `dashboard.py`'s template. A merged CSS/JS/markup change silently left the local `dashboard.html` stale until someone remembered to run the full `scripts/refresh_dashboard.py`, which also refetches live data over the network just to pick up a template change (this happened today, right after the light/dark theme PR merged).
- **[Data]** Extract CSS/JS into real source files, inlined at generation time -- Implements the recommended candidate from `plans/issue-51-extract-css-js.md` (full investigation there): keep CSS and JS as real source files with real tooling, but keep the served `dashboard.html` exactly as self-contained as it is today.
- **[Feature]** Plan issue #55: cloud-native transfer-deadline email reminder -- Investigation plan for #55 (email a transfer-deadline reminder with recommendations). Adds `plans/issue-55-deadline-email-reminder.md` recording the candidate trigger mechanisms and email-delivery mechanism considered, and the decision reached so far.
- **[Data]** Decline Opta Analyst as a data source (issue #13) -- Investigated whether Stats Perform's Opta Analyst site (theanalyst.com) could feed the projection model advanced metrics beyond the official FPL API. Full findings already recorded in `plans/issue-13-opta-feasibility.md`; this PR finalizes the decision by recording it in `IMPLEMENTATION_PLAN.md`.
- **[Feature]** Add guest (no-login) access path to cloud-hosting plan -- Per the user's requests to (1) think through a guest (no-login) access path, (2) confirm resequencing so guest access ships before the OAuth/storage work, and (3) make persistent-profile registration not require an email by default — this records all three in `plans/issue-27-cloud-hosting.md` and updates issues #44/#45/#46/#55 accordingly. Plan-doc-only change; no code.
- **[Feature]** Plan issue #46: implementation shape for the per-request team-view path -- Investigation plan for #46 (split refresh pipeline: shared FPL data vs. per-user computation). The strategic shape (#46 ships first, no dependency on #44/#45) was already settled in `plans/issue-27-cloud-hosting.md`; this pass works out *how* to actually build it against the real `server.py`/`dashboard.js` code, plus a terminology fix from a follow-up conversation with the user. Plan-doc-only change; no code.
- **[Data]** Split refresh pipeline: shared FPL data vs. per-team lookup computation -- Closes #46.
- **[Feature]** Per-team profile storage, no registration required (closes #45, merges #44) -- Closes #45.
- **[Feature]** Plan issue #65: backtest ML challengers to the projection model -- Investigates issue #65 (explore ML-based challengers to the projection model, backtest-first, shadow-mode candidate for 2026-27). Backtests three candidates against the existing 4-season historical corpus in `data/history/` -- no live season data needed.
- **[Feature]** Plan: security review before public hosting -- Plan doc for #28 — no implementation yet, per this repo's `plan-issue` convention of landing the design before the build.
- **[Feature]** Plan: preseason draft-squad recommendations -- Plan doc for #61 — no implementation yet, per this repo's `plan-issue` convention of landing the design before the build.
- **[Feature]** Plan: opt a team out of derived lookup recommendations -- Plan doc for #62 — no implementation yet, per this repo's `plan-issue` convention of landing the design before the build.
- **[Feature]** Plan: extend forecast-accuracy history tracking beyond one team -- Plan doc for #64 — no implementation yet, per this repo's `plan-issue` convention of landing the design before the build.
- **[Feature]** Investigate team rotation index as a model input (issue #11) -- clean negative result -- Implements the investigation from `plans/issue-11-manager-style-investigation.md` for issue #11 (PL team manager playing style as a model input). Adds one new stdlib-only research script, `scripts/investigate_team_rotation.py`, mirroring `scripts/investigate_ict_index.py`'s structure and rigor. No changes to `src/`, `config/`, or `data/`.
- **[Feature]** Lookup opt-out: first-claim PIN, gated explicit-lookup path (#62) -- Implements issue #62's recommendation: a manager can opt their team's derived recommendations out of the no-signup explicit-lookup path (`?team_id=`, issue #46), protected by a self-chosen, first-claim PIN, since #44/#45 shipped without any identity/credential system to lean on.
- **[Feature]** Preseason draft squad declaration and personalized feedback (#61) -- Implements the plan in `plans/issue-61-preseason-draft-squad.md`: before Gameweek 1's deadline, FPL's public API hides every manager's real picks, so the dashboard's Weekly Decision panel only ever shows the shared, non-personalized "fresh-squad benchmark". This lets a manager declare a self-built 15-player draft squad and get personalized transfer-style feedback on it in the same panel, ahead of the season.
- **[Feature]** Extend forecast-accuracy history tracking to every saved team profile -- Implements plans/issue-64-multi-team-history.md's recommendation in full.
